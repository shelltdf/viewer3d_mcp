# 数据库与存储设计

## 适用性说明

本仓库 **无服务端数据库**。以下为浏览器侧“数据”约定。

## 本地与缓存

- **IndexedDB / localStorage**：MVP 不强制使用；可选用于记住上次 URL（若实现则写入 `02-physical` 规格）。
- **模型与贴图**：由 glTF/GLB 及相对路径解析；跨域资源需服务端 CORS（若从远程 URL 加载）。

## 文件格式

- **glTF**（`.gltf` + 外部资源）与 **GLB**（单文件）均需支持。
