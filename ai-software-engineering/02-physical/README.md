# 物理阶段总索引

| target-id | 产物类型 | 说明 |
|-----------|----------|------|
| [gltf-mcp](./gltf-mcp/) | 静态 SPA（`dist/`）+ MCP stdio + HTTP 桥接 | Vue + Vite + three.js；与 Cursor MCP 经 `gltf-mcp-bridge` 通信（模式同 mindmap 扩展） |

源码根路径（相对实现子项目）：见各目标 `README.md`。
