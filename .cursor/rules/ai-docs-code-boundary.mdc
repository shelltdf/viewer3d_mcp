---
description: AI 文档与实现产物目录边界约束
alwaysApply: true
---

# 目录边界：AI 文档与实现产物必须分离（强制）

## 强制规则

- **AI 文档落盘目录固定**：所有由 AI 生成或维护的工程文档，必须保存在 `ai-software-engineering/` 目录内（包含其子目录）。
- **被管理的实现产物必须在文档目录之外**：所有被本项目管理的“实现产物”（包含但不限于代码、实现侧文档、资源文件、配置、构建脚本等）必须位于 `ai-software-engineering/` 目录之外。
- **只在必要位置创建目录**：若需要新增 AI 文档目录，仅在 `ai-software-engineering/` 下创建；若需要新增实现产物目录，必须在该目录外创建。

## 冲突与保护策略（不得破坏现有工程）

- **发现越界写入请求时必须拦截**：
  - 请求将实现产物（代码/资源/配置/实现侧文档等）写入 `ai-software-engineering/` 内：拒绝直接写入，并给出可行替代路径建议。
  - 请求将 AI 文档写入 `ai-software-engineering/` 外：拒绝直接写入，并建议迁移到该目录内。
- **仅提示，不破坏**：不得删除、移动、重命名用户已有文件；只报告冲突与最小人工动作。
- **历史遗留例外**：若仓库中已存在历史越界文件，默认不自动改动；在回复中列出风险并给出迁移建议。

## 回复要求

每次涉及文档或代码文件创建/修改时，回复中需明确：

- 文档写入路径是否位于 `ai-software-engineering/` 内
- 实现产物写入路径是否位于 `ai-software-engineering/` 外
- 若有冲突：冲突路径与最小人工处理动作

