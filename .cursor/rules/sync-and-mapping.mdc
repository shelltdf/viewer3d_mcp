---
description: 目标项目内强制同步：四阶段 ↔ 代码（产品映射）
alwaysApply: true
---

# 目标项目：四阶段文档与代码强制同步（强制）

本规则用于放在**目标项目目录**内的 `.cursor/rules/` 下，约束目标项目的文档与代码更新逻辑。

## 核心原则（必须遵守）

- **文档与代码是同一最终产品的不同映射**：二者必须最终一致。
- **允许修改任意文件**：用户可以直接修改目标项目内任意文件（四阶段文档或源码）。
- **强制同步**：任何修改都触发同步尝试，直到没有不一致项或明确阻塞原因。

## 目录结构（文档根：`ai-software-engineering/`）

- `ai-software-engineering/00-concept/`：概念（人类主笔）
- `ai-software-engineering/01-logic/`：逻辑（AI 为主）
- `ai-software-engineering/02-physical/`：物理（AI 为主）
- `ai-software-engineering/03-ops/`：运维（AI 为主）

### 物理阶段目录节点 `02-physical/`（强制布局）

物理阶段按**构建目标**（与 `CMake`/`dotnet`/`cargo` 等产出的可交付物一一对应）组织目录，**禁止**仅用根目录下一个长文 `*.md` 替代分目标规格（历史单文件仅允许保留**迁移/跳转**说明）。

1. **根索引（必须）**  
   - 路径：`ai-software-engineering/02-physical/README.md`  
   - 内容：列出本仓库全部目标子目录 `<target-id>/`、产物类型（`exe` / `dll` / 静态 `lib` 等）、一句话说明；可选表格维护。

2. **按目标子目录（必须）**  
   - 路径：`ai-software-engineering/02-physical/<target-id>/`  
   - 目录名：仅 **ASCII**（示例：`helloworld-exe`、`foo-lib`、`my-plugin-dll`）。  
   - 每个目标对应**一种**主要构建产物或强绑定的一组产物（若一组库需分文档，可再分子目录或拆 `spec-*.md`，但须在根索引中说明）。

3. **每个 `<target-id>/` 内最少制品（必须）**  
   - `README.md`：产物**文件名**、类型、**源码根路径**（相对实现子项目，如 `gui_test_cpp/apps/helloworld/`）、与 `CMake` 目标名对应关系（若可）。  
   - `spec.md`：该目标的**字段级/行为级**物理规格、**重要过程**（关键路径步骤、约束、边界）；本目标在物理层的**单一事实来源**以本文件为主，可与 `mapping.md` 交叉引用。  
   - `uml-*.md`：**若干**（至少覆盖一类 UML），**分文件**命名，建议前缀 `uml-`（如 `uml-class.md`、`uml-component.md`、`uml-sequence-*.md`）；可用 Mermaid / PlantUML / 导出图；**不得**替代 `00-concept/ui.svg` 的 UI 几何权威。  
   - `mapping.md`（**推荐**）：「模型元素 → 源码路径/符号」表；若合并入 `spec.md`，须在 `README.md` 中指明章节，避免无法检索。

4. **与实现代码目录的边界**  
   - `02-physical/` **仅**放 AI 工程文档；**实现产物**仍在 `ai-software-engineering/` 之外（参见 `ai-docs-code-boundary.mdc`）。同步时「物理文档 ↔ 实现子项目」通过 **路径引用** 对齐，不在文档目录内生成代码。

5. **同步时的回写落点**  
   - 代码 → 文档：至少更新**受影响目标**的 `spec.md`，并视变更更新对应 `uml-*.md` / `mapping.md`；若新增/删除构建目标，**必须**更新 `02-physical/README.md` 与子目录。

### 概念阶段 `00-concept/`（文件数量与命名）

1. **产品设计（唯一）**  
   - 全仓库在概念阶段只应有**一份**「产品设计」主文档，约定文件名为 **`product-design.md`**（ASCII）。  
   - 承载产品愿景、范围、用户/用例级总览、与业务相关的 UI/活动概述等；**不**因子系统或模块再并列第二份「产品设计」主文件。若需拆分章节，应在 **`product-design.md` 内分节**，或经人类明确同意采用**单一**替代文件名并在 `00-concept/README.md` 中声明其为产品设计唯一入口。

2. **软件设计、接口设计、数据库设计（可多份）**  
   - **`software-design*.md`**、**`database-design*.md`**：允许**多个**文件并存（示例：`software-design.md` 与 `software-design-helloworld.md`；或使用子目录 `00-concept/software-design/` 下多篇，目录名 ASCII）。  
   - **`interface-design*.md`（接口设计，范围限定）**：**仅**描述**程序间**接口形态，例如 **HTTP/REST**、**gRPC**、**RPC**、**进程间 IPC**（管道、套接字、命名管道、共享内存、COM、消息队列等）及**对外服务协议**、**SDK 公开 API** 的形状与约定。**不包含** GUI 人机交互：窗口与控件操作、菜单/快捷键、鼠标键盘流程、Win32 消息等**不得**写入 `interface-design*.md`；此类内容归 **`product-design.md`**、**`software-design*.md`**，以及 **`ui.svg`** 等概念资产与 **`01-logic`** 中与 UI 壳相关的详细设计。若无任何 API/RPC/IPC 类接口，可保留极简占位或标注「不适用」，**不要**用 GUI 操作说明充填该文件。  
   - 上述多文件用于按模块、子系统、交付物或话题拆分；新增时须在 **`00-concept/README.md`**（推荐维护）中列出索引与一句话职责，避免重复与冲突。  
   - **初始化最低范例**仍建议至少各有一份基线文档（见下表），后续可按上条规则扩展。

3. **同步基线（最低范例，缺失则初始化时补齐；不得覆盖已有同名文件）**

| 类型 | 最低文件（示例） | 说明 |
|------|------------------|------|
| 产品设计（唯一） | `product-design.md` | 产品总览 |
| 软件设计（可增） | `software-design.md` | 默认可含主程序 + 库等约定；可再增加多篇 |
| 数据库/存储（可增） | `database-design.md` | 广义存储；可再增加多篇 |
| 接口（可增） | `interface-design.md` | **仅** API / RPC / IPC 等程序间接口；**非** GUI；可再增加多篇 |

### 逻辑阶段 `01-logic/`（系统设计 与 详细设计 分文档）

逻辑阶段必须把**系统设计**与**详细设计**拆成**不同文档**（或不同文件集合），二者粒度不同：**系统设计**偏全局结构与边界；**详细设计**偏子模块/话题的展开，通常篇幅大、**允许多文件**。

1. **系统设计（一般一份即可）**  
   - 约定主文件名为 **`system-design.md`**（ASCII），作为本阶段**系统级**单一入口；也可经人类同意改用唯一文件名，但须在 **`01-logic/README.md`** 中声明为系统设计唯一入口。  
   - 内容：子系统/大模块划分、职责与依赖关系、与 `00-concept` 的一致性、与 `02-physical` 各 `<target-id>` 的对应关系（逻辑视角，**非**字段级规格）。**不**应用多份并列的「系统设计主文档」替代分节；若需展开，在 **`system-design.md` 内分章**或拆到「详细设计」侧。

2. **详细设计（多份，按子模块/话题拆分）**  
   - 命名建议：**`detailed-design-*.md`**（如 `detailed-design-playback.md`、`detailed-design-ui-shell.md`），或使用子目录 **`01-logic/detailed/`**（目录名 ASCII）下按模块再分文件。  
   - 内容：关键用例、业务流程、状态与交互、逻辑层数据与消息流、与活动图/序列图草案等；**按子模块或业务域**分别成文，避免单文件过长。  
   - 新增/变更时须在 **`01-logic/README.md`** 中维护索引（文件名 + 一句话职责）。

3. **综合设计文档（可选）**  
   - 跨子系统、端到端或专题综述（如性能策略、安全与权限、迁移路线）可单独成文，建议命名含 **`integrated`**、**`overview`**、**`cross-cutting`** 等可识别前缀，或统一放在 **`01-logic/synthesis/`**（ASCII）下；**不得**与 `system-design.md` 职责重复到无法区分——须在 `README.md` 中说明与系统设计的边界。

4. **同步与回写**  
   - 变更影响全局结构时优先改 **`system-design.md`**；影响某一子模块时改对应 **`detailed-design-*.md`**（或 `detailed/` 下文件）；跨域则改综合文档或同时改多处。  
   - 代码 → 文档：逻辑层细节至少落到**对应详细设计**文件；若牵动模块边界，同步 **`system-design.md`**。

### 各阶段角色与内容基线（完整度要求）

以下用于约束「四阶段」各自应承载的制品类型；同步时若发现缺项，应在同步尝试中**补齐或显式标注阻塞原因**（不得覆盖已有同名文件，除非人类确认）。

#### `00-concept/`（概念：人类主笔）

- **产品设计唯一**、**软件/接口/数据库设计可多份**：见上文 **「概念阶段 `00-concept/`（文件数量与命名）」**；其中 **`interface-design*.md` 不含 GUI**，仅 API/RPC/IPC 类程序接口。
- 概念阶段不替代物理层的字段级规格；与 `01-logic` / `02-physical` 冲突时按本规则同步流程消除。

#### `01-logic/`（逻辑：系统设计 + 详细设计）

- **分文档约定**：见上文 **「逻辑阶段 `01-logic/`（系统设计 与 详细设计 分文档）」**——**系统设计**一般以 **`system-design.md`** 为单一主文档；**详细设计**按子模块拆成多篇 **`detailed-design-*.md`**（或 `detailed/` 子目录）；可选 **综合**文档或 `synthesis/`。  
- **系统设计**：子系统/模块边界、职责划分、与概念阶段的一致性（落在 `system-design.md`）。  
- **详细设计**：关键用例、业务流程、状态与交互、逻辑层数据与消息流（落在各详细设计文件；可与活动图/序列图草案配合）。  
- 逻辑层表述「系统/模块如何协作」，**不**替代物理层 `02-physical/<target-id>/spec.md` 的字段级规格。

#### `02-physical/`（物理：代码 ↔ UML + 重要过程）

- **目录布局**：必须遵守上文 **「物理阶段目录节点 `02-physical/`（强制布局）」**；每个目标独立 `02-physical/<target-id>/`，根索引 `02-physical/README.md`。
- **UML（或等价结构化模型）与代码的映射**：包/模块/类（或文件级单元）/主要接口与实现路径之间应**可追溯**；落在各目标的 `mapping.md`（推荐）或 `spec.md` 内，并配合 `uml-*.md`。图可用 PlantUML、Mermaid、`*.drawio` 导出、`*.svg` 等。
- **重要过程说明**：对性能、正确性、安全或用户体验有关键影响的路径须写入对应目标的 **`spec.md`**（步骤、约束、边界条件与实现一致）。
- **单一事实来源**：对实现可见的接口形状、数据结构、错误码/错误语义，以各目标 **`spec.md`**（及映射表）为权威；与逻辑/概念冲突时按本规则同步流程消除。
- **UI 几何**：若以 `00-concept/ui.svg` 为权威，物理阶段 UML **不得**替代该 SVG。

#### `03-ops/`（运维：用户说明书 + 开发维护说明书）

- **用户说明书**：终端用户视角的安装、环境前提、启动与日常使用、功能与快捷键、常见问题与自助排障等。
- **开发维护说明书**：开发者视角的仓库结构、构建/测试/运行/发布入口、依赖与版本、配置、调试与排障、变更与发布约定等。
- 两类文档可拆成多文件（例如 `user-manual.md` 与 `developer-manual.md`）或分章节写在同一文件；**名称不强制**，但职责必须可区分。

## 触发规则（任何修改都触发）

当发生以下任一情况，必须执行“全量同步尝试”：

- 修改了 `ai-software-engineering/00-concept/`、`ai-software-engineering/01-logic/`、`ai-software-engineering/02-physical/`、`ai-software-engineering/03-ops/` 下任意文件
- 修改了源码实现（任意 `src/`、`app/`、`backend/`、`frontend/`、`*.ts/*.py/*.go/...` 等）

## 全量同步尝试（必须按顺序做）

1. **文档 ↔ 文档**：把变更级联到其余三个阶段，保持四阶段一致；逻辑层与物理层、运维层之间的**设计粒度**（`01-logic` 中 **系统设计与详细设计** vs `02-physical` 实现映射 vs `03-ops` 用户/维护）不得互相矛盾。
2. **文档 → 代码**：若已存在实现代码，按文档对齐实现（接口、数据模型、行为、错误码等）；各 **`02-physical/<target-id>/`** 中已给出的 **UML/模型 ↔ 代码** 映射与 **`spec.md` 重要过程**须可被实现验证。
3. **代码 → 文档**：若代码中存在文档未覆盖的关键细节（真实字段、边界条件、默认值、错误处理、模块边界等），必须回写到合适阶段；**至少**将对外行为与实现细节同步到 `02-physical/<target-id>/`（更新 **`spec.md`**，必要时 **`mapping.md`** 与 **`uml-*.md`**），并更新根索引 **`02-physical/README.md`**；逻辑层协作与流程细节回写到 **`01-logic/`** 下对应 **`detailed-design-*.md`**（或 `detailed/`），若牵动模块边界则同步 **`system-design.md`**；将用户可见与维护者必需的信息反映到 `03-ops`（用户说明书 / 开发维护说明书）。
4. **同步摘要**：每次回复必须给出：
   - 本轮修改了哪些路径
   - 同步完成了哪些项
   - 未完成项/阻塞点/需要的最小人工动作

## 强制更新口令

用户下达以下指令之一时，必须立即执行完整“全量同步尝试”，不得只做局部修补：

- “强制更新”
- “强制同步”
- “把文档和代码全部对齐”

## 同步命令：文档 ↔ 实现项目（基于修改时间）（强制）

用户下达以下指令之一时，必须把 `ai-software-engineering/` 与**某个具体实现子项目**之间的信息做一次双向同步，并在发生冲突时提示人类处理：

- “同步”
- “同步文档和代码”
- “sync”

### 适用范围与前置约束

- **多实现并存**：当仓库存在多个实现子项目（目录可由用户自定义）时，同步必须**明确目标实现目录**；若用户未指定，优先选择最近修改的实现目录，并在摘要中写明选择依据（修改时间）。
- **目录边界**：实现产物必须位于 `ai-software-engineering/` 之外（参见 `.cursor/rules/ai-docs-code-boundary.mdc`）；若检测到越界文件，必须停止自动同步并列出冲突路径与最小人工处理动作。

### 新旧关系判定（必须）

使用“文件修改时间”作为新旧关系的默认依据（不要求绝对精确，但必须可解释）：

- **文档更新更晚**：以文档为准推进 `文档 → 代码`（实现跟随物理阶段/接口/数据模型/错误语义对齐）。
- **实现更新更晚**：以实现为准推进 `代码 → 文档`（把关键真实细节回写到合适阶段，至少覆盖到对应 **`02-physical/<target-id>/`** 的 `spec.md` / 映射与 UML）。
- **无法可靠判定**（例如：双方都有最近修改、或存在明显语义冲突）：进入“冲突”流程，提示人类处理。

### 冲突处理（必须提示人类）

遇到任一情况都视为冲突，必须停止自动决策并提示人类最小动作：

- 同一概念在文档与实现中给出**不同的字段/默认值/边界条件/错误语义/接口形状**，且无法通过单向覆盖安全解决
- 发现多实现之间相互矛盾，且用户未指定以哪一个实现为准
- 检测到可能的破坏性变更（例如：更改公共接口但缺少迁移/兼容策略）

冲突提示必须包含：

- 冲突的文件路径（文档侧 + 实现侧）
- 需要人类做出的选择（以文档为准 / 以实现为准 / 手动合并）
- 建议的最小合并策略（例如新增兼容字段、保留旧接口并标记弃用等）

## 路径与编码约束（强制）

- **代码路径/文件名**：必须英文/ASCII，不得中文。
- **文本文件编码**：默认 UTF-8；确有工具限制的例外需明确说明，并优先选择可移植替代方案。

