---
description: 运行时依赖与分发资产的 License 归集到仓库根 THIRD_PARTY_LICENSES*（不含仅 dev 依赖）。
globs: "**/package.json", "**/pnpm-lock.yaml", "**/package-lock.json", "**/Cargo.toml", "**/go.mod", "**/THIRD_PARTY_LICENSES.md", "**/THIRD_PARTY_LICENSES.txt", "**/licenses/**"
alwaysApply: false
---

# 第三方 License 归集（强制）

本规则约束仓库在**交付/运行**层面涉及的**第三方可执行代码与数字资产**的许可证披露：须**自动或半自动归集**，并**落盘到项目根目录**，便于合规审查与分发说明。

## 范围界定

### 必须纳入（运行时 / 交付物）

- **运行时被调用或随产物分发的第三方代码**：例如被打包进 `dist`、随安装包发布的 **npm `dependencies`**、浏览器端 **CDN 引入且进入构建产物** 的库、**运行时动态加载**的脚本（若存在）。
- **运行时分发的数字资产**：例如随应用提供的**字体**、**图标集**、**纹理/贴图**、**音频**、**来自第三方包或明确授权的资源文件**（含其 `LICENSE` / `NOTICE` 要求一并保留或摘要）。

### 明确排除（仅开发期，不强制纳入根目录归集）

- **仅开发、构建、测试、Lint 使用**的工具链：`devDependencies`、本地 CLI、预处理器、测试框架、格式化工具等**不进入最终用户可运行产物**的依赖。
- **纯文档与 CI 配置**：除非其许可证条款要求随二进制分发（极少见），否则不强制写入根目录 license 汇总。

> 若某依赖同时用于开发与运行（边界模糊），**按保守原则**：只要**最终产物**中仍包含其代码或资源，则**纳入**。

## 落盘位置（项目根）

- **主汇总文件（必须）**：在**仓库根目录**（与 `package.json` / 实现子项目根同级）维护至少一份可读汇总，建议文件名（ASCII）二选一或并存：
  - `THIRD_PARTY_LICENSES.md`（人类可读，推荐），或
  - `THIRD_PARTY_LICENSES.txt`
- **可选补充**：根目录下 `licenses/` 目录，存放从 `node_modules` 等复制的原始 `LICENSE`/`NOTICE` 片段或按包分文件，**须在主汇总文件中索引**这些路径，避免散落不可追溯。

实现子项目若独立交付（多 `package.json`），**每个可交付根**各自一份汇总，或在主仓库根用一节说明子项目路径与对应汇总文件。

## 收集方式（实现提示）

- **Node / npm / pnpm**：以**生产依赖**为准（如仅统计 `dependencies`，排除默认的 `devDependencies`）；可用生态工具生成 SPDX 或列表后再整理进 `THIRD_PARTY_LICENSES.md`（具体命令随栈选型，须在项目 `README` 或开发者文档中写清**可复现的一条命令**）。
- **其他栈**（Cargo、NuGet、Maven 等）：按该生态**等价于「runtime / production」** 的依赖集合收集 license 文本或 SPDX 标识。
- **数字资产**：凡**随构建拷贝到 `dist` 或安装包**的资源，在汇总中单独小节列出：**来源**、**许可证名称**、**是否要求署名**、**仓库内路径或包名**。

## 同步义务

- **变更依赖或替换资源**（升级 major、新增包、更换字体/贴图）：必须**更新**根目录 license 汇总（或对应 `licenses/` 子文件），直至与当前**实际进入产物的**第三方集合一致。
- **回复中建议说明**：本轮是否触及依赖/资源变更、是否已更新 `THIRD_PARTY_LICENSES*`、未完成项与阻塞原因。

## 非目标

- 不替代法律顾问对许可证兼容性的结论；汇总以**事实收集与可追溯**为最低标准。
- 不要求为**未随产物分发**的开发依赖维护与运行时同级的根目录汇总（见上文排除项）。
