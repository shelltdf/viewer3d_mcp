---
description: 逆向工程、反向生成文档、reverse engineer docs——仅在此类口令下执行；输出到 reverse-engineering 独立目录，不改动实现侧源码树。
alwaysApply: false
---

# 逆向工程：从既有项目源码生成四阶段文档（按需触发）

本规则用于把**已经存在的项目源码/实现产物**逆向为 `ai-software-engineering/` 下的四阶段文档，帮助补齐文档、沉淀真实行为。

## 触发规则（必须严格遵守）

**只有当用户明确下达“逆向工程”指令时才执行本规则**。例如：

- “逆向工程”
- “反向生成文档”
- “把这个项目逆向成四阶段文档”
- “reverse engineer docs”

除上述明确指令外（包括日常“同步/强制同步/初始化项目”等），**禁止自动启动逆向工程**。

## 非破坏性约束（强制）

- **不得修改、移动、删除**任何既有实现侧文件与目录（例如 `src/`、`app/`、`backend/`、`frontend/`、用户自定义实现目录、构建脚本、配置、README 等）。
- 逆向生成的文档必须写入 `ai-software-engineering/` 内，但必须放在**独立目录**，不得覆盖现有四阶段主线文档。

## 输出目录约定（强制）

逆向工程输出必须写入以下目录之一（均视为“独立目录”）：

- `ai-software-engineering/reverse-engineering/<target-impl>/<run-id>/`

其中：

- `<target-impl>`：目标实现目录的安全标识（仅 ASCII；例如 `apps_nodejs` 或 `implementations_nodejs`）
- `<run-id>`：本次逆向的唯一标识（建议 `YYYYMMDD-HHMMSS` 或带简短 slug）

若目标输出目录已存在，必须创建新的 `<run-id>` 子目录，**不得覆盖**。

## 逆向工程产出范围（强制：最小闭环）

在独立输出目录内，必须生成最小闭环四阶段文档（可用最小可用内容，但必须可读、可追溯）：

- `00-concept/`：从实现反推“产品是什么/给谁用/成功标准”，标注为“逆向草案”
- `01-logic/`：关键用例、业务流程、状态/交互草案（以源码与可验证行为为依据）
- `02-physical/`：真实接口形状、数据模型、默认值、边界条件、错误语义（尽量引用源码位置）
- `03-ops/`：真实运行方式、环境变量、构建/启动命令、部署要点（以脚本/配置为依据）

同时必须生成一个总览入口文件：

- `README.md`：说明本次逆向的目标实现、逆向范围、证据来源（文件路径）、已知不确定点

## 证据优先级与不确定性处理（强制）

- **证据优先级**：可执行入口/测试用例/接口定义 > 配置与脚本 > 注释与文档 > 代码推断
- 对无法从实现中可靠确定的内容，必须在文档中明确标注为“未确认/假设”，并列出需要人类确认的问题清单。

## 与主线四阶段文档的关系（强制）

- 逆向输出目录下的文档默认是**隔离草案**，不得自动并入主线 `ai-software-engineering/00-concept` ~ `03-ops`。
- 只有当用户进一步明确要求“合并/对齐/以逆向结果为准”时，才允许把逆向结果同步进主线四阶段（并按冲突流程提示人类选择）。

## 回复中的强制摘要项

每次执行逆向工程后，回复必须包含：

- 本次目标实现目录（输入）
- 逆向输出目录（输出）
- 逆向生成/新增的文件清单（路径）
- 发现的冲突/不确定点与最小人工动作

