---
description: 按 02-physical 生成或新增实现方案、多语言多前端并存、build.py test.py run.py publish.py 工程闭环。
alwaysApply: false
---

# 多实现项目：基于 02-physical 生成不同代码方案（强制）

本规则用于支持“同一套四阶段文档 → 多套代码实现方案并存”的工作方式：例如同时维护 `C++`、`Java`、`Node.js`、以及不同前端实现（React/Vue/桌面端等）。这些实现应当是**独立的子项目目录**，各自遵循该技术栈的典型工程结构与文件管理风格。

## 核心原则（必须遵守）

- **单一事实来源**：实现的需求与对外行为以 `ai-software-engineering/02-physical/`（物理阶段）为主；若与 `00/01/03` 冲突，按 `.cursor/rules/sync-and-mapping.mdc` 执行全量同步尝试并消除冲突。
- **实现产物不得写入文档目录**：任何代码、构建脚本、依赖文件、实现侧 README、资源文件等都必须位于 `ai-software-engineering/` 之外（参见 `.cursor/rules/ai-docs-code-boundary.mdc`）。
- **可共存、互不覆盖**：不同实现放在不同子目录，禁止把一个实现的文件“混写”到另一个实现目录中；禁止跨实现互相覆盖文件（除非用户明确要求做迁移/合并）。
- **工程化最小闭环**：任何新建/新增的实现子项目必须提供最小可用的 `build / test / run / publish` 操作入口；默认通过同目录下的 `build.py`、`test.py`、`run.py`、`publish.py` 提供。`dev.py` 仅对 Node.js 等需要常驻开发态（watch/热重载）的实现强制要求（见下文“工程操作闭环（强制）”）。

## 实现目录约定（按用户指定）

- **无固定目录名**：不要求、也不预设诸如 `apps/`、`cpp/` 等**专用**实现目录名；由用户在仓库或工作区内**自行指定**放置代码的目录。
- **未指定时的默认**：若用户**没有**给出实现目录，则以**项目根目录**（仓库根、工作区根，`.`）作为本次实现的落点；在同步摘要中必须写明“未指定目录，使用项目根”。若仓库中已存在多个实现且需新增一套，仍应在根下使用**不同子目录**区分，避免互相覆盖。
- **唯一硬约束**：实现产物必须位于 `ai-software-engineering/` **之外**（参见 `.cursor/rules/ai-docs-code-boundary.mdc`）。落在项目根时，不得将代码写入 `ai-software-engineering/` 内。
- **当次任务**：必须**优先**使用用户明确给出的路径；仅当用户未指定时，才采用上文的**项目根默认**，不得在规则或文档中把其他目录名升格为“仓库标准”。

可选示例（仅说明常见工程形态；**未指定目录时**前缀为项目根，**已指定时**前缀为用户路径）：

- `<项目根或用户指定>/cpp/`：C++ 子项目（典型：CMake、`src/`、`include/`、`tests/` 等）
- `<项目根或用户指定>/java/`：Java 子项目（典型：Maven/Gradle、`src/main/java`、`src/test/java` 等）
- `<项目根或用户指定>/nodejs/`：Node.js 子项目（典型：`package.json`、`src/`、`test/` 等）
- `<项目根或用户指定>/frontend-react/`：React 前端（典型：Vite/Next.js）
- `<项目根或用户指定>/frontend-vue/`：Vue 前端

约束：

- **目录/文件名必须 ASCII**（与现有规则一致）
- **每个实现子项目必须自洽**：可独立安装依赖、构建、运行与测试（在能力范围内提供最小可用入口）

## 工程操作闭环（强制）

对每一个实现子项目（目录由用户指定；**未指定则为项目根**）都必须具备下列操作的**可执行入口**。默认要求在子项目目录下提供：

- `build.py`
- `test.py`
- `run.py`
- `publish.py`

按技术栈需要可额外提供 `dev.py`。其中 Node.js 实现默认必须提供 `dev.py`（用于开发态常驻流程）。这些脚本可以封装技术栈原生命令（例如 `cmake`、`npm`、`gradle`、`dotnet` 等），但必须满足“在该子项目目录下可复现执行”。

- **build**：能从干净状态构建出可运行产物（例如可执行文件、jar、dist 包等）
- **test**：至少提供一个 smoke test（可非常轻量，但必须能验证关键对外行为与退出码）
- **dev**：提供开发态入口（例如 watch/热重载/调试配置/增量构建）；若该技术栈不适合 dev server（如纯 CLI），可用 “debug 构建 + 运行” 作为 dev 入口，但必须明确
- **run**：以默认参数运行，并展示/验证输出（或 UI 现象）
- **publish**：提供“可分发产物”的最小路径（例如 `cmake --install`、`dotnet publish`、`npm pack`、`gradle assemble`、`jar` 输出等）；如无法在当前环境完成签名/安装器等高级发布，允许提供最小可用 publish（生成可拷贝运行的目录/文件）并注明限制

### 外部依赖/包管理（强制：默认自动下载）

当实现需要外部库或包时，默认策略为：

- **使用该生态的标准依赖管理器自动下载**（例如 npm/pnpm、NuGet、vcpkg/conan、Gradle/Maven、CMake FetchContent 等），并把依赖声明写入项目文件中
- **构建/测试/运行命令必须包含或隐含依赖安装/还原步骤**（例如 `npm install`、`dotnet restore`、`vcpkg install`、`cmake -DFETCHCONTENT...`）
- **可追溯性**：必须在实现子项目的 `README.md` 中明确写出依赖获取方式与最低版本要求；若依赖拉取需要网络或代理，也必须注明
- **失败处理**：若因离线/网络受限导致无法自动下载依赖，必须停止伪造成功，改为在同步摘要中列出阻塞点与最小人工动作（例如配置代理、预装依赖、提供离线镜像等）

## 生成/更新实现的触发与流程（必须按顺序）

当用户提出以下任一请求时，视为“生成或更新某个实现方案”的任务：

- “按 `02-physical` 生成 C++/Java/Node.js/某前端技术栈实现”
- “新增一个实现方案并与现有实现共存”
- “把同一需求用另一种语言/框架再实现一份”

必须执行：

1. **读取并理解物理阶段**：以 `ai-software-engineering/02-physical/` 为准，明确模块、接口、数据模型、错误语义与默认值。
2. **选定目标实现子项目目录**：优先使用用户指定目录；若用户未指定，则使用**项目根目录**作为实现根，并在摘要中说明。若目录不存在则创建；若已存在则只在该目录内增量修改。
3. **生成典型项目骨架**：按该语言/框架的“典型风格”组织文件与配置（依赖管理、构建、运行入口、最小示例）。
4. **同步回写（如需要）**：若实现过程中发现 `02-physical` 缺少关键细节（字段/边界/错误码/默认值/协议细节），必须回写到合适阶段文档并触发全量同步尝试（遵循 `.cursor/rules/sync-and-mapping.mdc`）。
5. **同步摘要要求**：每次回复必须额外说明：
   - 选择/创建了哪个实现目录（按用户指定；未指定则为项目根）
   - 本轮影响的是“新增实现”还是“更新既有实现”
   - 是否对 `02-physical` 做了回写，以及回写的原因

