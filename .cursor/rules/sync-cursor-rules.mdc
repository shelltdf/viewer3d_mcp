---
description: 同步 rules、同步 cursor rules、更新 rules、rules 对齐、从别的项目复制了规则——迁入规则后对当前仓库做对齐与验证。
alwaysApply: false
---

# 同步 Cursor Rules（从外部更新后对齐当前项目）

本规则用于：`.cursor/rules/` 中的规则**可能在其他仓库维护**，通过复制/合并迁入本仓库后，必须对**当前项目**做一次可追溯对齐，避免「规则已更新但仓库仍按旧假设运作」。

## 执行方式（强制：以 AI 对话为主）

- **主路径**：用户在 **Cursor 聊天窗口** 下达「触发口令」后，助手必须在**当前对话会话内**完成本规则列出的步骤：自行读取/对比规则与仓库、必要时修改 `ai-software-engineering/` 与实现代码、在力所能及范围内执行验证命令，并输出「回复中必须包含的摘要」。
- **不得**把「请用户去终端运行某个脚本」当作同步操作的默认替代方案；用户要的是**在 AI 窗口里完成对齐**，而不是被支开去跑命令。

## 与「文档/代码同步」的边界

- **`sync-and-mapping.mdc`**：四阶段文档 ↔ 实现代码的映射同步。
- **本规则**：**规则文件本身**迁入/更新后，对当前仓库的**规则落地与工程一致性**检查；若检查中发现文档/实现与新规冲突，再触发 `sync-and-mapping.mdc` 的全量同步。

## 触发口令（用户下达任一则必须执行本流程）

- 「同步 rules」「同步 cursor rules」「更新 rules」「rules 对齐」
- 「从别的项目复制了规则」「规则在其他项目改了再拷过来」
- 「逆向工程」「反向生成文档」等**不**触发本规则（见 `reverse-engineering.mdc`）。

## 必须执行的步骤（按顺序）

1. **清点与基线**
   - 列出当前 `.cursor/rules/` 下所有 `.mdc`（及若存在的 `AGENTS.md`）文件名与修改时间（或 git 状态）。
   - 若用户说明了**来源路径/来源仓库**，记录为「外部基线」；未说明则仅针对当前工作区做「迁入后」对齐。

2. **差异与意图**
   - 若存在 git：用 `git diff`（或用户提供的补丁）标出本轮相对上一提交的规则变更摘要（新增/删除/重命名规则文件、frontmatter 变更、关键条款变更）。
   - 无 git 时：按文件逐项阅读，写出「新规相对旧认知」的 3～10 条要点。

3. **项目级对齐（强制）**
   - **路径与目录假设**：检查规则中写死的路径（例如 `ai-software-engineering/`、实现目录名）是否与本仓库一致；不一致则**更新规则正文中的路径表述**或在本仓库增加**最小占位说明**（例如在 `README.md` 中单段说明「本仓库文档根目录为 …」），禁止只改规则不检查仓库是否满足。
   - **`alwaysApply` / `globs`**：确认每条规则的适用范围是否仍适合本仓库；若某规则应改为「仅编辑某类文件时生效」，调整 frontmatter 并说明原因。
   - **与现有规则冲突**：若两条 `.mdc` 对同一行为给出矛盾指令，必须列出冲突点，并给出**合并方案**（删、改、或拆分为更细规则），不得留隐含矛盾。

4. **工程后果**
   - 若新规要求初始化结构、边界目录或多实现脚本等：对照 `init-project-structure.mdc`、`ai-docs-code-boundary.mdc`、`multi-implementation-projects.mdc` 检查本仓库是否已满足；不满足则**按新规补齐最小结构或脚本**，或明确标注「阻塞原因 + 最小人工动作」。
   - 若规则变更会改变文档/代码同步义务：在完成规则对齐后，触发 **`sync-and-mapping.mdc`** 的「全量同步尝试」（文档 ↔ 文档 → 文档 ↔ 代码）。

5. **验证（力所能及）**
   - 若规则涉及构建/测试命令：在说明环境限制的前提下，**由助手尝试执行**相关命令或等价检查，失败则写入摘要中的阻塞项。

## 回复中必须包含的摘要

- 本轮处理的是哪些规则文件（路径列表）。
- 相对迁入前的**变更要点**（条列）。
- 针对本仓库已做的**路径/结构/ frontmatter** 调整。
- 是否已触发或需要触发 **文档/代码全量同步**（参见 `sync-and-mapping.mdc`）。
- **未完成项 / 需人工决策**（若有）。

## 非目标（避免范围蔓延）

- 不自动从网络拉取其他仓库的规则；仅处理**已出现在本仓库 `.cursor/rules/`**（及用户明确粘贴）的内容。
- 不删除用户未要求删除的既有规则文件；删除或重命名需用户确认或冲突处理说明。
