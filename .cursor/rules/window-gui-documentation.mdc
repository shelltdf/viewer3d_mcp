---
description: 窗口程序与 IDE/Webview 内嵌 GUI 的文档落点与同步义务；若产品含自有文件格式/DSL，亦约束 IDL 版本化与兼容保证工程约定（非 API 载荷字段级 spec）
alwaysApply: true
---

# 窗口程序 GUI 说明（强制）

本规则约束**带图形窗口**的桌面/Web 壳程序（如 Win32 / WPF / WinUI / Electron / 浏览器内嵌 GUI 等）在工程文档中的**界面说明**写法与同步，避免与「程序间接口」文档混淆。**若产品定义自有文件格式或自有 DSL**，并采用 **IDL（接口描述）** 管理**该自有契约**的兼容，则须同时遵守下文 **「IDL / DSL 与自有文件格式兼容」**；**若仅使用公共标准或第三方格式、不由本仓库维护规范**，则**不要求**维护自有 `idl/` 版本链（见该节「适用范围」）。IDL 的**工程流程与落盘**归本规则，**载荷字段、序列化字节级协议**仍以 `interface-design` / `02-physical` 为权威，二者不得互相替代。

## 窗口程序类型：SDI 与 MDI

文档中描述 GUI 时，应**显式**标明本产品采用的壳层类型（或混合形态），便于读者理解窗口层级与多文档行为。

- **SDI（Single Document Interface，单文档界面）**  
  - **含义**：典型形态为**一个顶层主窗口**承载当前工作区；一次聚焦「一份」主文档（或单一编辑会话）。打开另一份文档时，常见实现为**再开一个顶层窗口**，或**在同一主窗口内切换**当前文档（标签页等仍属 SDI 范畴，但须在文档中说明是「单窗口多标签」还是「一文档一窗口」）。  
  - **文档建议写明**：顶层窗口数量策略、新建/打开文档时是否新开窗口、关闭最后一个文档时是否退出等。

- **MDI（Multiple Document Interface，多文档界面）**  
  - **含义**：存在**一个主框架/父窗口**（容器），**多份子文档**以**子窗口**（或等价嵌入视图）形式在**客户区内**并列/层叠；菜单栏/工具栏往往挂在**父框架**上，子窗口有标题栏（或标签）切换。  
  - **文档建议写明**：父窗口与子窗口的职责划分、子窗口排列方式（层叠/平铺/标签）、活动子窗口如何影响菜单与状态栏等。

- **混合与变体**：若实际为「浏览器式单窗口 + 多标签 SDI」或「工具窗口浮动 + 主 SDI」，在概念/软件设计文档中**用一两句话界定**，避免与经典 MDI（父子框架）混淆。

- **IDE 内嵌编辑器（VS Code / Cursor / JetBrains 等）**：顶层窗口常为 **IDE 外壳**；本产品编辑器 UI 若在 **Webview / BrowserView / iframe 内嵌页** 中绘制，在文档中应标明为 **「单窗口多标签 SDI」**：每个编辑器标签对应一个 **文档或会话**，**客户区**多为 IDE 中央编辑区内的 **页面内区域**（HTML/CSS），而非独立顶层 `HWND` 应用窗口。新建/关闭标签与 IDE 行为一致时，可引用宿主说明并**补充**页面内专属行为（未保存、自定义标题圆点等）。

### IDE 宿主内 Webview / 浏览器内嵌页（强制补充）

当**菜单栏、工具栏、底部状态栏等主要交互区位于 HTML 页面内**（Electron `BrowserWindow` 加载的页面、VS Code `WebviewPanel`、类 PWA 壳等），而非操作系统或 IDE **顶层**菜单栏时，须遵守以下**文档写法**，避免机械套用本节「纵向分区」而误以为必须在 **OS/IDE 顶层**再实现一套同名菜单：

| 要求 | 说明 |
|------|------|
| **壳层分工（强制）** | 在 `00-concept` 相关文档中**至少一段**写清：哪些 UI 属于 **IDE 宿主**（例如：工作台标签标题、IDE 状态栏条目、IDE 主题色），哪些属于 **页面内客户区**（例如：Webview 内顶部菜单栏、画布、页内状态栏）。二者**不要**在 GUI 专章混称为同一套原生控件。 |
| **纵向分区映射** | 上文「菜单栏 / 工具栏 / 客户区 / 状态栏」**允许**全部映射到 **页面内 DOM 分区**；名称与线框以 `00-concept/ui.svg` 或产品文档为准，并可与实现中的容器 `id` / `class`（如 `statusbar`、`dock-*`）交叉引用。 |
| **多语言 / 主题（职责不变）** | **须**在**用户可直达的页面内交互**中提供（通常为页内 **Language / Theme** 顶层菜单或等价入口）。默认语言、默认主题策略仍须满足本节「菜单栏最低要求」表中的**意图**（中/英与跟随系统）；若**初值**同步自 IDE API（如 `vscode.env.language`、IDE 颜色主题），须在文档中说明**与页内切换**的优先级及是否持久化到工作区/用户配置。 |
| **状态栏与 Log（职责不变）** | 若**可点击打开 Log** 的是 **页面内底部状态栏**，本节「状态栏与 Log 窗口」要求**直接适用**。若产品将摘要放在 **IDE 状态栏**、详细日志仅在 **输出通道 / Debug Console**，须在 `03-ops` 与概念文档中**显式说明**该拆分、是否提供页内纯文本 Log + 全文复制；**不得**在未实现页内可追溯 Log 时声称已满足「状态栏点击即完整历史 Log」——应标注**缺口或后续计划**。 |
| **Dock / Dock Area** | 页内 **Dock Area（停靠区域）** 与 **Dock（停靠窗口）** 见下文专节；须说明与 **IDE 原生侧栏**（资源管理器、扩展视图等）是否独立，以及「最大化编辑区」类命令是否联动隐藏 IDE 部件（若实现）。**Dock View**、**折叠按钮区域（Dock Button Bar）**、分割条等约定**仍适用**于页内布局。 |
| **2D 画布** | 客户区内 **二维主画布** 见下文 **「2D 画布（客户区主绘制区）」**：中键平移、滚轮缩放、**指针位置为缩放中心**；**左上**快捷键说明面板（**可折叠，默认折叠**，语义与右上要素可见性面板一致）、**左下**比例与加减及 **适应 / 原点 / 还原**、**右上**要素可见性面板（**可折叠，默认展开**）；与页内 **Toolbar** / **Dock** 叠层避让须在文档或 `02-physical` 可检索。 |
| **Toolbar** | 页内 **Toolbar（工具栏）** 见下文专节；**位于 Dock Area 之外**；**分割按组成组**，非每钮一分割；四边停靠、多工具栏、溢出悬浮条、**浮动**等以专节为准。 |
| **不属于 GUI 说明** | Webview **CSP**、`vscode-resource` / `asWebviewUri` 等资源协议、**程序间**消息载荷字段——归 `interface-design` / `02-physical` spec，**不**替代本节对**可见区域与操作路径**的描述。 |

### Windows 风格壳层共同特征（SDI / MDI 共用）

采用经典 **Windows 桌面应用**式主框架时，文档与实现宜对齐下列**纵向分区**（与具体技术栈无关，Electron/Web 仿壳亦可类比）：

| 区域 | 位置 | 说明 |
|------|------|------|
| **菜单栏** | 最上方（标题栏下方） | 命令入口：文件/编辑/视图/帮助等 |
| **工具栏** | 菜单栏之下（经典纵向布局） | 高频操作的快捷入口；**四边停靠、多条工具栏、与 Dock Area 关系等**见下文 **「Toolbar（工具栏）」专节** |
| **客户区** | **中间**主区域 | 主工作区：画布、编辑区、MDI 子窗口区等；**二维主画布**的交互与角区叠层控件见下文 **「2D 画布（客户区主绘制区）」** |
| **状态栏** | 最下方 | 提示、光标/缩放、就绪状态等 |

- **经典纵向示意**：上表描述**最常见**的上下顺序；若产品实现 **Toolbar** 贴左/右/下缘或 **浮动**，须在 GUI 说明中写清与菜单栏、状态栏、**Dock Area** 的相对位置，并以 **Toolbar 专节**为准。
- **客户区**：文档中应使用术语 **客户区**（client area）指称上述中间主工作区，避免与「中键（鼠标）」混淆。
- 若产品偏离该经典布局（如侧边栏主导、无菜单栏），须在概念/软件设计文档中**显式说明**差异与理由。

### 2D 画布（客户区主绘制区）

本节约束**客户区内以二维平移/缩放为主**的主画布（脑图、示意图、平面编辑等）；**纯列表/表单主导**、无统一视口变换的界面可标注**不适用**或**仅部分适用**。

#### 指针与视口操作（强制）

- **中键平移**：须允许使用 **鼠标中键拖拽**（或产品定义的等价中键手势）对画布视口进行 **平移（pan）**。
- **滚轮缩放**：须允许使用 **鼠标滚轮** 对画布视口进行 **缩放**。
- **缩放中心（强制）**：滚轮缩放时，**缩放中心须为缩放发生时刻鼠标指针在画布上的位置**（指针锚点缩放）；**不得**在文档未说明的情况下默认改为「仅以视口几何中心缩放」等与上述语义不符的策略。若产品**另行提供**「以视口中心缩放」等模式，须在 `00-concept` / `02-physical` / `03-ops` 中写明**默认值**、**切换入口**与优先级。

#### 画布角区叠层与缩放条（强制）

以下控件为叠在 **画布视口** 上的 **overlay**（典型 `position: absolute` / 等价），**以画布可视区域为参照**；与 **Toolbar**、**Dock Area**、页内 **状态栏** 的几何关系若有重叠风险，须在概念线框或 `02-physical` 中固定避让规则。

| 位置 | 控件与职责（名称可本地化，**语义须一致**） |
|------|--------------------------------------------|
| **左上角** | **快捷键说明面板**：与 **右上角要素可见性面板** 同属画布 overlay **可折叠面板**（标题条/折叠钮 + 主体区，**affordance** 以实现为准）。**主体区**内展示 **完整快捷键列表**（纯文本或可滚动区域，便于阅读与选中）；**不得**仅以「悬停才出现的悬浮层」作为**唯一**查看方式——须支持 **显式折叠 / 展开**（与右上面板一致的交互模型）。**默认状态为折叠**（首次进入或未记录状态时，仅显示标题条/折叠柄，列表主体隐藏）。**折叠 / 展开** 按钮的悬停提示须符合上文「菜单/按钮与快捷键悬停提示」（无命令快捷键时标明无全局快捷键等）。若 **折叠状态** 须持久化，须在 `02-physical` / `03-ops` 写明存储键与范围。画布上其他可点击缩放/视口控件的 **悬停快捷键提示** 仍须符合该小节。 |
| **左下角** | **缩放比例**：以 **数字** 展示当前缩放比例（如 **百分比**），与视口实际缩放**一致**、**可读**。 |
| **左下角（比例两侧）** | **缩小（−）**、**放大（+）**：分列于比例数字 **左**与**右**（或产品等价横向排列）；步进与上下限以 `02-physical` 为准；**有快捷键时悬停须展示**。 |
| **左下角（在上述控件之上）** | **三个按钮**（单独一行或成组置于比例/加减 **上方**）：**适应**——调整视口使画布内 **当前应纳入的全部内容** 均落在可视范围内（fit all / zoom to fit，边界定义以 `02-physical` 为准）；**原点**——在 **不改变** 当前缩放比例 的前提下，将 **平移** 调整为使 **当前图元包围盒中心** 与 **视口中心** 对齐（与「适应」的 **居中** 段采用同一几何语义，但 **不** 重算缩放；避免与「仅将变换平移置零」混同导致根/整图飞出视口）。无图元时的退化策略由 `02-physical` 约定；**还原**——将 **缩放比例设为 100%**，且以 **当前视口中心** 为锚点进行缩放复位（视口中心下的画布点尽量保持在视口中心），避免用户注视区域被突然甩到角落。 |
| **右上角** | **要素可见性面板**：罗列画布内 **可按类型** **显示 / 隐藏** 的要素类别（如网格、辅助线、某类图元/图层等）；每项提供可切换的可见性控制；须在文档中给出 **类型列表**、**默认值**及各要素是否 **持久化**（工作区/用户配置）。**面板容器**须支持 **折叠** 与 **展开**（标题条/柄、`details`、抽屉头等 **affordance** 以实现为准）；**默认状态为展开**（用户首次进入或未记录状态时，面板主体可见）。若 **折叠/展开状态** 亦须持久化，须在 `02-physical` / `03-ops` 写明存储键与范围；若不持久化，须在文档中说明（例如仅本会话或每次打开重置为展开）。 |

#### 文档与同步

- **概念/线框**：四角区布局、与 **Dock** / **Toolbar** 的相对位置及 z-index 约定；**左上快捷键面板**（默认折叠）与 **右上可见性面板**（默认展开）的折叠柄位置与展开态占位。
- **`02-physical`**：适应算法的 bounds、原点定义、100% 与指针锚点缩放的数值语义、可见性类型枚举；**左上快捷键面板默认（折叠）**、**右上可见性面板默认（展开）** 及各自 **折叠状态持久化**（若有）约定。
- **`03-ops`**：中键/滚轮、左下按钮组、**左上快捷键面板**（如何折叠/展开、默认折叠）与 **右上可见性面板**（如何折叠/展开、默认展开）的用户说明。

### Toolbar（工具栏）

**Toolbar**（**工具栏**）承载高频命令入口。文档与线框中须将其与 **Dock Area / Dock** 区分：**工具栏在 Dock Area 之外**，不占用 **折叠按钮区域** 或 **Dock View**。

#### 停靠位置与用户调整（强制）

- **四边停靠**：工具栏可附着在主窗口 **上 / 下 / 左 / 右** 四边之一；产品可允许多条工具栏分处不同边。
- **拖动换边**：用户可通过拖动调整工具栏在 **上 / 下 / 左 / 右** 之间的归属（拖柄、整条工具栏拖动等 **affordance** 由产品定义，须在文档中说明）。

#### 与菜单栏、状态栏的边界（强制）

在**含菜单栏与状态栏**的主框架中，**停靠态**工具栏须满足：

- **上缘**：不得延伸到**菜单栏之上**或与菜单栏重叠（工具栏整体位于菜单栏**之下**或与之并列于产品约定的「顶区」内，但**不得越过菜单栏上边界**）。
- **下缘**：不得延伸到**状态栏之下**或与状态栏重叠（工具栏整体位于状态栏**之上**或与之并列于产品约定的「底区」内，但**不得越过状态栏下边界**）。

文档须用**示意图或文字**固定本产品的 **菜单栏 → Toolbar（若在上/顶区）→ …客户区与 Dock… → 状态栏** 的层级关系；若左右缘停靠，须说明与顶/底 Toolbar、菜单栏、状态栏的**不交叉占用**规则。

#### 与 Dock Area 的相对位置（强制）

- **Toolbar** 位于 **Dock Area 之外**：不得将工具栏实现为 **Dock View** 或 **折叠按钮区域**的一部分；与 **Dock Area** 相邻时的分割条、阅读顺序须在 `00-concept` / 线框或实现 `id`/`class` 中可检索说明。

#### 默认呈现与分组分割（强制）

- **默认以图标为主**。
- **分割（separator）按组使用，而非逐按钮**：**不要求**在**每一个**按钮之间都放分割；仅在**产品/用户需求**下，将**语义相关**的命令划为 **一组**，在**组与组之间**插入分割以示区隔。**无分组必要**时，工具栏可 **全程无分割**。
- 若分组关系对用户不直观，须在 GUI 说明、`00-concept` 线框或 `03-ops` 中简要写明 **分组意图**（例如「文件一组 / 编辑一组」）。
- 若另提供「仅图标 / 图标 + 文字」等切换，须在 GUI 说明或用户手册中写明（与「菜单/按钮与快捷键悬停提示」一致）。

#### 多个工具栏（强制）

- 允许存在 **多个 Toolbar**。
- 当多个工具栏排在**同一行或同一条停靠带**内时，应 **自动按可用空间调整比例**（或按产品约定的加权比例），使并列工具栏共同填满该带；**最小/最大宽度或高度**若有，须在文档或 `02-physical` 中固定。

#### 溢出与溢出悬浮工具栏（强制）

- 当单行（或单条停靠带）**无法容纳**全部按钮时，须提供 **溢出指示**（典型为 **箭头** 或「更多」形按钮），表示**仍有命令未显示在当前带内**。
- 用户**点击**该指示后，应展示承载**溢出部分**的 **悬浮工具栏**（浮动条、弹出层或下拉面板等，语义等价即可），以便访问被隐藏项；文档与 `03-ops` 须写明：**如何打开/关闭**、是否点击外部关闭、以及与 **快捷键悬停提示** 的配合。

#### 浮动（强制）

- **Toolbar** 在 **不依附**主窗口四边时，可处于 **浮动**状态（独立浮动条或小型浮动窗口）；文档须说明 **停靠 ↔ 浮动** 的切换方式、拖拽限制与**重新停靠**路径。

#### Toolbar 浮动与 Dock 浮动（区分）

- **Dock** 除依附 **Dock Area** 外，亦可 **脱离停靠**并以 **浮动面板** 呈现（见下文「与浮动面板及 Toolbar 浮动」）。
- **Toolbar 浮动** 与 **Dock 浮动** 语义不同（前者为命令条，后者为工具/属性等内容面板），须在 GUI 说明中**分别**描述，避免混用术语。

### 菜单栏最低要求（强制）

采用含**菜单栏**的主框架时，菜单栏中**至少**包含下列两类顶层菜单（名称可本地化，职责须一致）：

| 菜单 | 职责 | 默认值（强制） |
|------|------|----------------|
| **多语言** | 切换界面显示语言 | **默认**至少支持 **中文** 与 **英文**；可扩展其他语言，须在文档中列出 |
| **主题** | 切换浅色/深色等界面主题 | **默认**与**当前操作系统/系统**的外观或主题设置**保持一致**（跟随系统）；若无法实现跟随，须在文档中说明实际策略与阻塞原因 |

- 文档与实现应对齐：**语言列表**、**主题选项**、**默认值**、以及「跟随系统」的检测方式（如 `prefers-color-scheme`、系统注册表/设置 API 等，按技术栈如实描述）。

### 菜单/按钮与快捷键悬停提示（强制）

- **范围**：凡**用户可点击**的**菜单项**（含下拉/上下文菜单中的条目）、**工具栏按钮**、**标题栏与其他等价命令按钮**，以及**与命令强绑定的可点击控件**（如缩放、全屏、**2D 画布**左下角 **适应 / 原点 / 还原** 与 **加减速率按钮**、**2D 画布**左上角 **快捷键面板** 与 **右上要素可见性面板** 的 **折叠 / 展开** 按钮、**Dock Button** / 折叠按钮区域入口等），均须在**鼠标悬停**时向用户展示**与该命令对应的键盘快捷键**（若存在）。
- **呈现方式**：以实现栈为准，但必须**可被用户在不点击的情况下发现**（典型：`title` / **ToolTip** / 平台等价控件）；**仅图标**的工具栏按钮**不得**省略快捷键提示（可写在悬停文案中与命令名一并出现）。
- **平台与修饰键**：文档与实现宜说明 **Windows/Linux** 与 **macOS** 的差异（如 `Ctrl` 与 `⌘`）；若某快捷键在宿主侧固定为 **Ctrl**（例如 IDE 扩展键位），悬停文案须与**实际可触发键**一致，**不得**写「有快捷键」却与真实绑定不符。
- **无快捷键时**：若某命令**确实无**全局或上下文字快捷键，悬停提示中应**显式标明**（如「无全局快捷键」或等价本地化文案），避免用户误以为遗漏。
- **文档同步**：`03-ops/user-manual.md`（或等价用户说明）中应概括：**悬停即可查看各命令控件上的快捷键说明**；**2D 画布**左上角 **快捷键列表**以 **可折叠面板** 内纯文本（或等价）集中展示，**不替代**其他可点击控件上的逐条悬停说明义务。
- **非目标**：本小节不要求为**纯展示/装饰**、**已禁用且无替代路径**的控件编造快捷键；不要求在**触摸设备无悬停**时单独再叠一层常驻文案（若产品仅触屏，须在文档中说明实际策略）。

### 状态栏与 Log 窗口（强制）

- **状态栏**须支持**点击**（整块或约定区域）后，弹出 **Log 窗口**（日志窗口）：用于展示**程序已输出的全部信息之历史**（可追溯、可滚动查看完整记录；是否分页/条数上限若有须在文档中说明）。
- **Log 窗口**须汇总程序内统一输出通道中的信息，**至少**包含：**提示**、**警告**、**错误**；若有其他级别或类别（如调试、信息），一并纳入同一 Log 流或在文档中区分。
- **Log 正文须为纯文本形态**（例如只读文本区、`<pre>` 或等价控件）：便于检索、选中和与「复制」结果一致；**禁止**仅以富文本/HTML 作为唯一载体导致无法直接得到纯文本。
- **复制**：Log 窗口须提供**复制**按钮（或等价明确命令），将**当前 Log 全文**（与界面可见纯文本一致）写入系统剪贴板，便于用户粘贴到邮件、工单或外部编辑器；文档与实现应说明复制范围（通常为全部历史或当前筛选结果，二选一须在文档中固定）。
- 文档与 `03-ops` 用户说明中应写明：打开方式（状态栏点击）、Log 与状态栏单行摘要的关系（若状态栏仅显示最近一条，仍须在 Log 中可查历史）、**纯文本展示与复制按钮**的用途。

### 脏文档（Dirty document）/ 脏缓冲区与保存状态指示（强制）

#### 术语（文档与实现应对齐）

| 英文 | 含义 |
|------|------|
| **Dirty document** / **dirty buffer** | 内存中的编辑内容相对**磁盘上已保存版本**已发生变化，或**尚未绑定落盘路径**且存在用户可丢失的编辑会话。 |
| **Clean** | 与磁盘（或已确立的持久化目标）一致，或无可丢失的未持久化编辑。 |

- 中文文档中可使用 **「脏」**、**「脏数据」**、**「脏文件」**（口语）、或对用户更友好的 **「未保存的更改」** / **「有未保存修改」**；技术讨论与代码注释可保留 **dirty** / **`isDirty`**。
- **勿**与「数据校验失败」意义上的「脏数据」混用；若上下文可能歧义，应写全 **「未保存（dirty）」**。

#### 三端共同要求（网页 / 套壳 / VS Code 窗口）

以下**至少**应有一种或组合可被用户直接感知（具体形态以实现栈为准，须在 `00-concept` / `03-ops` 写明）：

1. **编辑器标签或等价标题区**：未保存时常见 **圆点**、**「·」** 后缀、或宿主原生 **dirty 指示**（VS Code 文档标签与 `TextDocument.isDirty` 一致）。
2. **页内状态栏或专用指示**：例如与保存状态绑定的 **三色灯**（绿/黄/红）或等价图标 + `title` / `aria-label` 说明。
3. **离开或关闭前确认**：关闭标签页、刷新、关闭窗口或应用退出时，若存在未保存更改，须有 **`beforeunload` / 宿主保存提示 / 扩展拦截** 等机制之一。

- **IDE 宿主内 Webview**：须区分 **IDE 标签上的脏**（由 `TextDocument` / 定制编辑器同步）与 **页内画布状态**；文档中说明二者如何对齐（例如扩展侧 `_dirty` 与防抖写入 `TextDocument`）。
- **独立浏览器与 Electron 套壳**：无 IDE 标签时，**页内标题栏/状态栏**与 **关闭前确认** 的责任完全在页面或主进程侧，须在文档中单独写清。

#### 本仓库 `mindmap_vscode` 当前实现摘要（实现演进时须同步更新本节）

- **共用页面**（`mindmap_vscode/media/webview-app.js`）  
  - 画布变更调用 `markContentDirty()` → 置内部 `contentDirty` → 向宿主 `postMessage({ type: 'mindmap:edited' })`；保存成功路径调用 `setContentClean()`。  
  - `beforeunload`：若 `contentDirty` 则拦截关闭/刷新（浏览器标准确认框）。  
  - 页内 **`#statusbarSaveLight`**：`applySaveTrafficLight(light)` 根据 `'green' | 'yellow' | 'red'` 切换样式与 `title`。**VS Code** 由宿主 **`mindmap:saveTrafficLight`** 驱动；**浏览器 / Electron** 由 **`refreshLocalSaveTrafficLight()`**（与扩展语义对齐：黄=脏、红=干净但未关联磁盘、绿=干净且已关联路径）驱动，并接收 **`mindmap:hostFilePath`**（Electron 主进程）或浏览器内 **`localBackingPathKnown`** 维护。  
  - **`#mainRowSplitter`**：右侧三 Dock **全为折叠或关闭**时 **`updateMainRowSplitterInteractable()`** 为 **`#dockRightStack`** 加 **`mm-dock-stack-fold-only`**（**`#dockAreaView` 不显示**，仅 **缘条**），并为分割条加 **`mm-main-row-splitter-inactive`**（透明、不可拖，**保留与可拖时相同的水平占位**以免主区与 Dock 宽度跳动）；拖拽中若变为全折叠则由 **`mm-main-row-splitter-force-end-drag`** 结束拖拽。

- **VS Code / Cursor 扩展**（`mindmap_vscode/src/panel.ts`）  
  - `MindmapPanel._dirty` 与 **`mindmap:edited`** 联动；绑定 `TextDocument` 时通过同步逻辑使 **`document.isDirty`** 与工作台「脏标签」一致。  
  - **`WebviewPanel.title`**：无 `TextDocument` 绑定时，脏时在标题基名后加 **` ·`**；有绑定时以文档标签为准。  
  - **`_computeThisPanelSaveLight()`** 计算黄/红/绿 → **`postMessage({ type: 'mindmap:saveTrafficLight', light })`** → Webview 更新三色灯；**`mindmap:ready`** 后也会推送一次。  
  - **`setContext('mindmapEditorHasUnsavedChanges', …)`** 与 **`workspaceState`** 中 `mindmap.dirtyMindmapAtLastSync` 等用于关闭/退出与键位 `when` 子句。

- **独立浏览器**（无扩展宿主）：与扩展共用 **`webview-app.js`** 内 **`refreshLocalSaveTrafficLight()`**（`useLocalSaveTrafficLight()` 为真）：**黄**=`contentDirty`；**红**=干净但未关联磁盘路径（新建/未保存）；**绿**=干净且已打开或已保存（含 File System Access **`saveHandle`**、下载保存等路径由 `installBrowserMindmapHost` 维护 **`localBackingPathKnown`**）。依赖 **`contentDirty` + `beforeunload`** 拦截刷新；**`mindmap:savedOk`** 等价路径调用 **`setContentClean()`**。

- **Electron 套壳**（`mindmap_vscode/desktop/main.js`）：同上由页内 **`refreshLocalSaveTrafficLight()`** 计算三色灯；主进程在 **`currentFilePath` 变化**及 **`did-finish-load`** 时 **`sendHostMessage({ type: 'mindmap:hostFilePath', path })`**，页面据此置 **`localBackingPathKnown`**。保存成功后仍 **`mindmap:savedOk`** 清脏。关闭窗口由主进程 **`close`** 与 **`window.mmGetContentDirty()`** + **`dialog.showMessageBox`** 确认，**勿**依赖 **`beforeunload`+`preventDefault`** 拦截关闭。

- **VS Code 扩展**：由宿主下发 **`mindmap:saveTrafficLight`**。页内 **`useLocalSaveTrafficLight()` 为假**（含通过 `location` 识别 **`vscode-webview` / `vscode-file`**，避免 Webview 亦属 Electron 时误判为「桌面本地灯」）。

#### 文档与同步

- `03-ops/user-manual.md`（或等价）须说明：**如何看出当前是否未保存**（标签 / 标题 `·` / 状态栏灯 / 关闭提示）。  
- 修改脏状态或保存指示相关代码时，须**回写**本节或 `03-ops` 中与用户可见行为一致的描述。

### Dock Area 与 Dock（停靠区域与停靠窗口）

本节先定义两个**不同层级**的概念，再规定布局与交互；文档与实现中的命名宜与下表对齐（历史文档中的 **缘条 / edge strip** 对应 **折叠按钮区域**（**Dock Button Bar**）；**显示区 / display area** 对应 **Dock View**）。**Dock Area** 与 **Dock View**、**Dock Button Bar** 的父子与兄弟关系见下「**Dock Area 的组成与节点关系**」。

#### 术语定义

| 英文 | 中文 | 含义 |
|------|------|------|
| **Dock Area** | **停靠区域** | 主窗口内一侧的**容器区域**，可附着在窗口 **上 / 下 / 左 / 右** 四边之一（或产品允许的多边同时存在）；**一个 Dock Area 内可以容纳多个 Dock**。与客户区（主工作区）并列，由布局吸附在框架边缘。 |
| **Dock** | **停靠窗口** | **可承载具体 UI 内容**的单元（工具面板、属性表、资源库等）；**被添加到某个 Dock Area 中**（或经产品允许 **脱离停靠**后以**浮动**呈现，见下）。每个 Dock **独立**折叠/展开；可选配置 **排他性展开**（展开其一则自动收起同 Area 内其他 Dock，是否启用由产品需求决定）。 |
| **Dock Button** | **停靠按钮** | 位于 **折叠按钮区域**内，与**某一个 Dock** 一一对应；用于切换该 Dock 的展开/折叠。**展开与折叠状态下均保持可见**，通过**样式差异（如颜色）**区分状态。可有**图标**，也可无图标（仅文字或符号）。**排版上**仅与**条带内的按钮**发生对齐与尺寸关系，**不与 Dock View 的宽高耦合**（见下「行为关联与排版正交」）。 |
| **Dock View** | **Dock 显示区域** | 位于 **Dock Area** 内、与**折叠按钮区域**并列的**内容区**：用于展示**已展开**的 Dock 之**主体内容**。**排版上**仅与**其内部的 Dock 窗口/面板**发生对齐与分区关系，**不与折叠按钮区域或 Dock Button 的宽高耦合**（见下「行为关联与排版正交」）。 |

#### Dock Area 的两分区结构（强制）

每个 **Dock Area** 在布局上划分为两个**兄弟区域**（同一父级下并列，**勿**将折叠按钮塞进某一个 Dock 面板内部作为唯一实现）：

| 分区 | 中文 | 位置与职责 |
|------|------|------------|
| **Fold button strip** | **折叠按钮区域**（亦称 **Dock Button Bar** / **停靠按钮条**） | **紧贴窗口外缘**（左缘 / 右缘 / 上缘 / 下缘，依 Dock Area 所在边而定）。**仅**承载 **Dock Button**；多个 Dock 对应多个按钮，在条带内沿边排列。 |
| **Dock View** | **Dock 显示区域** | 与折叠按钮区域**并列**（同一 Dock Area 下的兄弟容器），位于折叠按钮区域与**主客户区**之间。**仅**承载**展开态**下各 Dock 的内容面板；**不包含** Dock Button 本身。并列**只表示**在 Dock Area 内的相邻分区，**不**表示二者在**垂直或水平方向上的尺寸须互相匹配或联动**。 |

#### Dock Area 的组成与节点关系（强制）

- **组成式**：**Dock Area** = **Dock View** + **折叠按钮区域**（**Dock Button Bar**，与 **Fold button strip** 同义）。
- **层级**：**Dock View** 与 **折叠按钮区域（Dock Button Bar）** 均为 **Dock Area** 的**直接子节点**（布局树 / DOM 中同一父容器下的两个并列子区域）。
- **兄弟关系**：**Dock View** 与 **Dock Button Bar** **互为兄弟**；**不得**将 **Dock Button Bar**（作为承载全部 **Dock Button** 的条带）实现为某一 **Dock** 或 **Dock View** 的**唯一内嵌子节点**——须与上文「两分区为兄弟、勿将折叠按钮塞进某一个 Dock 面板内部作为唯一实现」一致。

#### 折叠按钮区域与 Dock View：行为关联与排版正交（强制）

- **行为上唯一关联**：**Dock Button** 与对应 **Dock** 的 **Dock View 内展示**之间，**仅**存在 **折叠 / 展开** 的状态关联（点击按钮切换该 Dock 是否在 Dock View 中展示主体内容；标题栏折叠等与之语义一致）。**不得**因「要对齐某一种视觉」而把二者的**内部布局尺寸**强行绑定。
- **排版完全独立**：**折叠按钮区域** 与 **Dock View** 在 **宽度与高度上互不决定、互不跟随**——一方的内容变高、变宽、滚动或重排，**不**应成为另一方在正交方向上拉伸、压缩或重算尺寸的依据（除非产品显式采用**整条 Dock Area** 外层的统一约束，如 splitter 只动「Dock Area 整体相对客户区」的宽度，而非「条带高度随 Dock View 内容增高」这类耦合）。
- **对齐范围（强制）**：
  - **折叠按钮区域 / Dock Button**：**仅**对 **条带内部的 Dock Button**（及条带内允许的间隔、缘条装饰）做对齐与分布；**不对齐** Dock View 内的 Dock 窗口边缘，也**不以** Dock View 内某一 Dock 的高度/宽度为参照去拉伸条带。
  - **Dock View**：**仅**对 **其内部的 Dock（停靠窗口）** 做分栏、堆叠、滚动与对齐；**不对齐** 折叠按钮区域内的按钮列/行，也**不以** 某 Dock Button 的尺寸为参照去强制本区宽度或高度。
- **排序与条带内规则（强制）**：多个 **Dock Button** 在条带内的排列顺序仍按 **按钮自身尺寸与产品约定规则**，**不受 Dock View 内面板布局变化的影响**。**Dock View** 内的分区与滚动**不受折叠按钮区域**尺寸变化的牵制（与上文「正交」一致）。
- **阅读顺序（实现提示）**：**左侧** Dock Area 建议 DOM/阅读顺序为 **折叠按钮区域 → Dock View → 客户区**；**右侧**为 **客户区 → Dock View → 折叠按钮区域**；**上/下**边同理，折叠按钮区域贴**最上/最下**。

#### Dock View 内多 Dock 与空间利用

- **同侧多 Dock**：同一 **Dock View** 内可同时展示多个已展开的 Dock 时，应按产品约定分配空间（如纵向或横向分栏）；须**尽可能充分利用**可用区域。
- **滚动**：当同时展示的 Dock 内容超出可视区域时，须在 **Dock View** 内提供 **滚动条**（或等价滚动容器），便于用户**滚动查看与微调**可见范围。
- **单独折叠其一**：某一 Dock 折叠后，其余仍展开的 Dock 应能**占满**该 Dock View 剩余空间（文档中可写明「折叠其一后其余扩展」）。

#### Dock Button 行为（强制）

- **始终可见**：无论对应 Dock 处于展开还是折叠，**Dock Button 始终显示**；通过**颜色/图标/文案**等区分展开态与折叠态。
- **沾满方向（实现与文档）**：Dock Button **不必**在无关维度上拉伸占满整条折叠按钮区域条带（与旧版「缘条按钮不沾满」规则一致）；且**不得**为与 **Dock View** 或内部 Dock 窗口「对齐高度/宽度」而在无关维度上强行拉伸（见「行为关联与排版正交」）。

#### 每个 Dock 的标题栏（强制）

每个 **Dock** 须有**自己的标题栏**（或等价标题行），至少提供下列能力（名称可本地化，**职责须一致**）：

- **折叠**：收起该 Dock 的主体内容（与 **Dock Button** 为**同一 Dock** 的折叠/展开状态联动；**仅为行为联动**，不改变「折叠按钮区域与 Dock View 排版正交」约束）。
- **最大化**：在该 Dock Area 的 **Dock View** 可用范围内将该 Dock **占满**（或产品约定的「该侧最大化」）；再次触发须能**还原**。若技术栈无法实现，须在文档中说明。
- **关闭**：关闭或隐藏该 Dock（若产品提供「关闭」）；**关闭后须仍可通过菜单恢复**（见下节）。

#### 主菜单：窗口菜单与 Dock 可发现性（强制）

- 在**主菜单栏**的 **「窗口」**（或等价顶层菜单，如 **View → Appearance**）中，须**列出当前可用的各个 Dock**（或分组列出），使用户在 **关闭 Dock 之后仍能重新打开**，避免「再也找不到入口」。
- 文档与 `03-ops` 须写明：**从哪一级菜单可以重新显示被关闭的 Dock**。

#### Dock 在 Dock Area 之间移动

- **Dock** 应支持被用户**拖动**到**另一个 Dock Area**（例如从右侧移到左侧）；文档须说明**拖拽 affordance**（拖柄、标题栏拖拽等）、**合法目标边**与**非法放置**时的行为。

#### 排他性展开（可选）

- 产品可配置某一 Dock 或某一 Dock Area 为 **排他性展开**：展开一个 Dock 时**自动折叠**同区域内其他 Dock；**不配置**时则允许多个 Dock **同时展开**。须在概念/软件设计文档中写明默认策略与开关位置。

#### 与浮动面板及 Toolbar 浮动

- **默认**：**Dock Area** 与 **Dock** **依附主窗口内边**；**Toolbar** 在 **Dock Area 之外**（见上文 **Toolbar** 专节）。
- **Dock 浮动**：**Dock** 可支持 **不停靠**状态——**脱离 Dock Area** 后以 **浮动面板** 呈现；文档须说明 **拖回停靠**、**关闭后从菜单恢复**（与「窗口」菜单列出 Dock 一致）及与 **Toolbar 浮动** 的区分。
- **Toolbar 浮动**：**Toolbar** 的 **浮动命令条** 与 **Dock 浮动面板** 职责不同，须在 GUI 说明中**分条**描述，避免混称。

#### 边缘拖动（Splitters）（强制）

- **Dock Area** 与**主客户区**之间的分界（及产品定义的其它可调边）在**至少有一个 Dock 处于展开态**（其在 **Dock View** 内展示主体内容）时，须支持**鼠标拖动**改变宽度或高度（**splitter / 分割条**）。文档与 `03-ops` 须写明：拖动位置、光标提示（如 `col-resize` / `row-resize`）、最小/最大尺寸或比例（若有）；Web/跨浏览器若有限制须如实说明。
- **全折叠时隐藏可调边界（强制）**：当某一 **Dock Area** 内**全部** **Dock** 均处于**折叠**态（**无任何** Dock 在 **Dock View** 内展开主体；与 **Dock Button** 所表示的折叠语义一致）时，该 **Dock Area** 与**主客户区**之间的 **splitter / 分割条**须**不呈现**或**不响应**拖拽调尺寸（二者取一或组合均可，语义为：用户不可再通过该分界改变该侧 Dock Area 与主客户区的尺寸分配）。**任一** Dock 重新**展开**后，分割条须**恢复**呈现并可拖动（除非产品另有「整侧 Dock Area 隐藏」等独立状态，须在 `00-concept` / `02-physical` 写明是否与「全折叠」合并判定）。**关闭**（隐藏）的 Dock 是否仍占用该 Area 内名额、以及是否参与「是否全部折叠」的判定，须在 `02-physical` 与实现一致地固定。

#### 实现命名提示

- 代码或文档中常用 `Dock`、`DockArea`、`DockPanel`、`DockSite` 等命名；**含义以上述「Dock = 停靠窗口、Dock Area = 停靠区域」为准**；与操作系统「任务栏停靠」无关，勿混用。

## IDL / DSL 与自有文件格式兼容（若适用）

本节所述 **IDL 版本生成、`idl/` 目录下按版本落盘的快照**，**仅针对本仓库/本产品「自定义、且由我们自行演进」的文件格式或 DSL 契约**。若产品**直接采用公共（标准）格式**或**第三方格式**作为存储/交换载体，且**不修改、不 fork 该格式的规范定义**，则**不要求**、**不建议**在本仓库生成或维护自有 **IDL 版本序列**——格式规范由**标准组织或第三方**维护，本仓库只需在文档中**引用**其规范名称与版本（如 RFC、ECMA、厂商文档版本号），无需重复造「自有 IDL」。

### 适用范围：何时须维护 IDL 版本，何时不必

| 情形 | 是否生成本仓库 `idl/<N>.*` 版本快照 | 说明 |
|------|--------------------------------------|------|
| **自有格式 / 自有 DSL**（语法、字段、语义由本仓库定义并可能随版本变更） | **是**（按本节流程） | 需要可追溯的契约快照与跨版本转换依据。 |
| **公共标准格式**（如标准 JSON/XML、标准图像/音视频容器等，仅按标准读写，无本仓库专有扩展定义） | **否** | 以公开规范为准；文档中引用标准即可。 |
| **第三方格式**（由其他产品/库定义的格式，本仓库仅实现读写兼容，不承担规范维护） | **否** | 不复制第三方规范为「自有 IDL」；兼容性跟踪第三方发布说明。 |
| **在标准或第三方格式之上仅有「极薄封装」**（如固定 MIME、外层 zip 内为标准结构且无本仓库专有语义层） | **通常否**；若封装引入**本仓库独有**、标准未覆盖的语义字段，则对**独有部分**可单独维护小型 IDL 或扩展说明 | 须在 `00-concept` / `02-physical` 中写清边界，避免误把整文件标为「自有 IDL」。 |

当产品具备**自有文件格式**或 **DSL**（领域专用语言），并需要长期保证**跨版本读写兼容**时，推荐采用 **IDL**（Interface Description Language，接口描述；此处指对「格式契约」的机器可读描述，实现形态可为 XML/JSON/其他，以仓库约定为准）管理**与软件版本独立**的 **IDL 版本号**。

### 术语与版本独立性

| 术语 | 说明 |
|------|------|
| **IDL**（本节约束） | 描述当前格式契约的**规范快照**（可自动生成、可落盘、可对比）；**不是** GUI 线框，**不是**运行时 HTTP 报文字段表（后者归 `interface-design` / `02-physical`）。 |
| **DSL** | 产品内嵌或暴露的**领域语言**（文本/图形语法等）；其**语义与版本策略**应在概念/逻辑文档与 IDL 快照中可追溯。 |
| **IDL 版本号** | **从 1 开始的整数**，**独立于**应用程序 `package.json` / `CMake` / 安装包等**软件版本号**；仅当「格式契约相对上一版 IDL 快照发生**实质变化**」时递增。 |

### 兼容保证方式（强制流程）

1. **自动生成临时 IDL**  
   - 由代码或构建脚本根据**当前**格式规范/解析器/生成器定义，**自动生成**一份 **临时 IDL 描述**（实现为临时文件或内存流，名称与工具以仓库为准）。

2. **与「当前最新」已提交 IDL 对比**  
   - **对比对象**：源码目录下 **`idl/`** 目录中**版本号最大**（或约定为 `latest` 指针所指向）的**已提交** IDL 文件。  
   - **若规范化后等价**：认为**无契约变更** → **删除临时文件**，**不**递增 IDL 版本号，**不**写入新版本文件。  
   - **若不等价**：进入第 3 步。

3. **规范化后再判定「相同」**（避免无意义版本膨胀）  
   - **禁止**仅做原始字符串相等即判定为「相同」。须先经 **规范化（canonicalization）** 再比较，例如：XML 子元素按稳定键排序、去除无关空白差异、属性顺序归一、枚举值排序等（具体规则须在仓库脚本或文档中**固定**）。  
   - **若规范化后语义一致**（仅文本顺序/排版不同）→ 视为**相同** → **不**生成新 IDL 版本。  
   - **若规范化后仍不同** → 视为契约变更 → **IDL 版本号 +1**，并保存正式快照。

4. **落盘规则**  
   - 将新版本 IDL 以 **`idl/<N>.xml`**（或仓库统一扩展名）写入**源码目录**下的 **`idl/`**，其中 **`<N>` 为新的整数版本号**；**不得**覆盖历史版本文件。  
   - 若仓库采用 `idl/README` 或 `idl/latest` 指针，须在文档中说明更新策略。

5. **触发重新生成与对比的时机**  
   - **前提**：产品存在**自有格式/自有 DSL** 并已启用 `idl/` 流水线；**纯公共或第三方格式**不运行本节流水线。  
   - 用户或 CI 在以下变更后应触发「**生成临时 IDL → 规范化 → 与最新已提交 IDL 对比**」：  
     - 修改 **IDL 生成相关代码**、**自有格式规范文档**、**解析/序列化核心**、或 **自有 DSL 文法/映射规则**等（具体 glob 或路径由仓库在 `README`/开发者手册中列出）。  
   - 若对比结果需新版本：执行版本号 +1 与落盘；并在提交说明或变更日志中注明 **IDL 版本**。

### 跨版本兼容与转换

- **版本间迁移**（**自有格式**场景）：允许并鼓励使用 **AI 或手写**维护 **「IDL vA → vB」的转换程序**（独立小工具或库内模块），使旧文件/中间表示可迁到新契约，从而实现**跨 IDL 版本**兼容。  
- 文档中宜列出：**当前软件读取的最低/最高 IDL 版本**、**升级路径**（若需多跳转换须写明）。**公共/第三方格式**的兼容迁移遵循**外部规范**与第三方工具链，不要求自建 IDL 转换链。

### 文档与同步

- **概念/逻辑层**：在 `00-concept` / `01-logic` 中说明 **自有 DSL/自有格式** 与 **IDL 版本策略**（与软件版本解耦）；**仅公共/第三方格式**时写明所依规范来源即可。  
- **物理层**：`02-physical/<target-id>/` 可与 `idl/` 交叉引用；**字段级行为**仍以各目标 `spec.md` 为准。  
- **运维层**：`03-ops` 可说明用户侧「文件版本过旧/过新」时的提示或修复入口（若产品暴露给用户）。  
- 修改**自有格式**的 IDL 生成逻辑或 `idl/` 落盘内容时，须**同步**上述文档中与版本、兼容、转换相关的表述。

### 非目标

- 本小节**不**规定 IDL 的具体 XML Schema（由项目实现）；**不**替代 **OpenAPI/gRPC proto** 等外部 IDL 生态，除非产品明确采用。  
- **直接使用公共格式或第三方格式、且不由本仓库维护规范定义**时：**不**生成自有 **IDL 版本信息**，**不**因「用了 PNG/标准 JSON」等而在 `idl/` 下维护版本化契约快照；兼容与升级跟随**外部规范**。  
- 若产品**无**自有文件格式/DSL，或**全部为**标准/第三方格式承载，可忽略整节（或仅在文档中引用外部格式说明）。

## 定义（GUI 说明包含什么）

- **属于 GUI 说明**：主窗口与子窗口结构、菜单/**Toolbar（工具栏）**/状态栏/停靠区、对话框与向导、画布/列表/属性区等**区域划分与命名**；**Toolbar** 的四边停靠、与菜单栏/状态栏边界、**Dock Area 外**、**按组成组的分割（非逐按钮强制分割）**、多工具栏比例、**溢出箭头与溢出悬浮工具栏**、**Toolbar 浮动**（见上文「Toolbar」）；**2D 画布** 的 **中键平移**、**滚轮缩放与指针锚点缩放中心**、**左上快捷键说明面板**（可折叠、**默认折叠**）、**左下缩放比例与 ± 及 适应/原点/还原**、**右上要素可见性面板**（可折叠、**默认展开**，见上文「2D 画布（客户区主绘制区）」）；**IDE 与页面内客户区的壳层分工**（见「IDE 宿主内 Webview / 浏览器内嵌页」）；**多语言菜单**、**主题菜单**、**状态栏与 Log 窗口**（见上文强制小节）；**脏文档/未保存状态与保存指示**（见「脏文档（Dirty）/ 脏缓冲区与保存状态指示」）；**菜单/按钮悬停快捷键提示**（见「菜单/按钮与快捷键悬停提示」）；**Dock Area / Dock / Dock Button / Dock View** 的区域划分、**Dock Button 与 Dock View 仅折叠展开行为关联且排版正交（宽高独立、对齐各管内部）**、**边缘拖动调尺寸**、每个 **Dock** 标题栏 **折叠 / 最大化 / 关闭**、**Dock 浮动**（见上文「Dock Area 与 Dock」）；**鼠标/键盘/快捷键**与可见控件操作路径；**用户可见状态**（选中、禁用、加载中）与典型交互流程；若有权威 UI 几何/线框，引用 `00-concept/ui.svg` 等概念资产。
- **属于本规则文件、但不属于 GUI 专章**：**自有格式**场景下的 **IDL/DSL 兼容策略、`idl/` 目录版本化快照、规范化对比与跨版本转换**（见「IDL / DSL 与自有文件格式兼容」）；**公共/第三方格式**不适用自有 `idl/` 版本链。与 **`interface-design` / `02-physical`** 的边界见该节及下文「不属于」。
- **不属于 GUI 说明**（勿写入 GUI 专章来替代）：HTTP/gRPC/IPC/SDK 等**程序间接口**形态——此类归 `interface-design*.md`（参见 `sync-and-mapping.mdc` 对 `interface-design` 的界定）。**IDL 快照**描述的是**格式契约**，**不**替代运行时接口的**字段级/字节级**规格（仍以 `interface-design` / `02-physical` 为准）。

## 文档落点（与四阶段一致）

- **概念层**：`00-concept/product-design.md`、`software-design*.md` 中描述产品级界面与壳层划分；若有独立线框/图示，以 `ui.svg` 或概念资产为准。**若存在自有 DSL/自有格式**（非纯公共或第三方格式承载），补充 **IDL 版本策略**（与软件版本解耦）及与 `idl/` 的对应关系；**若仅为标准/第三方格式**，写明所依规范与外部版本即可，不要求 `idl/` 版本链。
- **逻辑层**：窗口内**模块协作与界面状态流**写在 `01-logic/` 的 `system-design.md` 或相应 `detailed-design-*.md`，避免与物理层字段级规格重复。**自有 DSL/自有格式演进与 IDL 差异**可落在详细设计侧，与 `idl/` 版本交叉引用；**标准/第三方格式**写明依赖版本与边界即可。
- **物理层**：**实现文件 ↔ 界面区域**的映射可写在对应 `02-physical/<target-id>/mapping.md` 或 `spec.md` 的「界面」小节（字段级仍以 `spec.md` 为权威）。**仅自有格式**时：**源码树 `idl/<N>.xml`（或约定扩展名）** 与生成脚本路径宜在 `spec.md` / `mapping.md` 或 `idl/README` 中可检索说明；**纯公共/第三方格式**不写自有 `idl/` 版本目录义务。
- **运维层**：用户可见的菜单、快捷键（含**悬停即可见的快捷键提示**策略）、**未保存（dirty）如何识别**、**语言与主题切换**、**Log 窗口**与常见问题可在 `03-ops/user-manual.md` 中给操作说明。**若面向用户暴露自有格式版本**，可说明与 **IDL 版本** 或兼容提示的关系；**若文件为标准/第三方格式**，说明所依外部规范即可。

## 同步义务

- **修改了窗口 GUI 实现**（例如 Vue/React/WPF/XAML/资源对话框等）：在力所能及范围内**回写或补齐**上述文档中与变更相关的 GUI 说明（含 **Toolbar** 停靠/浮动、溢出与 **Dock** 停靠/浮动；含**新增/变更菜单或按钮时**与「菜单/按钮与快捷键悬停提示」的一致性；含**脏状态/保存指示/关闭确认**与「脏文档（Dirty）/ 脏缓冲区与保存状态指示」及 `03-ops` 的一致性）；若与 `sync-and-mapping.mdc` 的全量同步冲突，按该规则执行冲突流程。
- **修改了自有格式的 IDL 生成逻辑、自有 DSL/格式规范、或 `idl/` 下已提交快照**：须按「IDL / DSL 与自有文件格式兼容」执行**规范化对比与版本递增**；并回写 `00-concept` / `01-logic` / `02-physical` / `03-ops` 中与 **IDL 版本、兼容、转换**相关的表述（若与 `sync-and-mapping.mdc` 冲突，按该规则执行）。**若变更仅涉及公共/第三方格式引用**（无自有契约变更），**不**触发自有 `idl/` 版本递增义务。
- **回复中建议简要说明**：本轮是否涉及界面变更、已更新哪些文档路径（若未更新须写明阻塞原因）。

## 非目标

- 不强制为每个按钮写长篇文案；以**可检索、可追溯**为最低标准。
- 不将本规则升格为「唯一 UI 真相」：若仓库以 `ui.svg` 或产品文档为几何权威，物理层 UML **不得**替代该权威（与 `sync-and-mapping.mdc` 一致）。
