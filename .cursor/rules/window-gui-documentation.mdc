---
description: 窗口程序 GUI 说明的文档落点与同步义务（人机界面，非 API）
alwaysApply: true
---

# 窗口程序 GUI 说明（强制）

本规则约束**带图形窗口**的桌面/Web 壳程序（如 Win32 / WPF / WinUI / Electron / 浏览器内嵌 GUI 等）在工程文档中的**界面说明**写法与同步，避免与「程序间接口」文档混淆。

## 窗口程序类型：SDI 与 MDI

文档中描述 GUI 时，应**显式**标明本产品采用的壳层类型（或混合形态），便于读者理解窗口层级与多文档行为。

- **SDI（Single Document Interface，单文档界面）**  
  - **含义**：典型形态为**一个顶层主窗口**承载当前工作区；一次聚焦「一份」主文档（或单一编辑会话）。打开另一份文档时，常见实现为**再开一个顶层窗口**，或**在同一主窗口内切换**当前文档（标签页等仍属 SDI 范畴，但须在文档中说明是「单窗口多标签」还是「一文档一窗口」）。  
  - **文档建议写明**：顶层窗口数量策略、新建/打开文档时是否新开窗口、关闭最后一个文档时是否退出等。

- **MDI（Multiple Document Interface，多文档界面）**  
  - **含义**：存在**一个主框架/父窗口**（容器），**多份子文档**以**子窗口**（或等价嵌入视图）形式在**客户区内**并列/层叠；菜单栏/工具栏往往挂在**父框架**上，子窗口有标题栏（或标签）切换。  
  - **文档建议写明**：父窗口与子窗口的职责划分、子窗口排列方式（层叠/平铺/标签）、活动子窗口如何影响菜单与状态栏等。

- **混合与变体**：若实际为「浏览器式单窗口 + 多标签 SDI」或「工具窗口浮动 + 主 SDI」，在概念/软件设计文档中**用一两句话界定**，避免与经典 MDI（父子框架）混淆。

### Windows 风格壳层共同特征（SDI / MDI 共用）

采用经典 **Windows 桌面应用**式主框架时，文档与实现宜对齐下列**纵向分区**（与具体技术栈无关，Electron/Web 仿壳亦可类比）：

| 区域 | 位置 | 说明 |
|------|------|------|
| **菜单栏** | 最上方（标题栏下方） | 命令入口：文件/编辑/视图/帮助等 |
| **工具栏** | 菜单栏之下 | 高频操作的快捷入口 |
| **客户区** | **中间**主区域 | 主工作区：画布、编辑区、MDI 子窗口区等 |
| **状态栏** | 最下方 | 提示、光标/缩放、就绪状态等 |

- **工具栏默认呈现**：以**图标**为主（仅图标或图标 + 悬停说明）；若默认带文字标签或提供「仅图标/图标+文字」切换，须在 GUI 说明或用户手册中写明。
- **客户区**：文档中应使用术语 **客户区**（client area）指称上述中间主工作区，避免与「中键（鼠标）」混淆。
- 若产品偏离该经典布局（如侧边栏主导、无菜单栏），须在概念/软件设计文档中**显式说明**差异与理由。

### 菜单栏最低要求（强制）

采用含**菜单栏**的主框架时，菜单栏中**至少**包含下列两类顶层菜单（名称可本地化，职责须一致）：

| 菜单 | 职责 | 默认值（强制） |
|------|------|----------------|
| **多语言** | 切换界面显示语言 | **默认**至少支持 **中文** 与 **英文**；可扩展其他语言，须在文档中列出 |
| **主题** | 切换浅色/深色等界面主题 | **默认**与**当前操作系统/系统**的外观或主题设置**保持一致**（跟随系统）；若无法实现跟随，须在文档中说明实际策略与阻塞原因 |

- 文档与实现应对齐：**语言列表**、**主题选项**、**默认值**、以及「跟随系统」的检测方式（如 `prefers-color-scheme`、系统注册表/设置 API 等，按技术栈如实描述）。

### 状态栏与 Log 窗口（强制）

- **状态栏**须支持**点击**（整块或约定区域）后，弹出 **Log 窗口**（日志窗口）：用于展示**程序已输出的全部信息之历史**（可追溯、可滚动查看完整记录；是否分页/条数上限若有须在文档中说明）。
- **Log 窗口**须汇总程序内统一输出通道中的信息，**至少**包含：**提示**、**警告**、**错误**；若有其他级别或类别（如调试、信息），一并纳入同一 Log 流或在文档中区分。
- **Log 正文须为纯文本形态**（例如只读文本区、`<pre>` 或等价控件）：便于检索、选中和与「复制」结果一致；**禁止**仅以富文本/HTML 作为唯一载体导致无法直接得到纯文本。
- **复制**：Log 窗口须提供**复制**按钮（或等价明确命令），将**当前 Log 全文**（与界面可见纯文本一致）写入系统剪贴板，便于用户粘贴到邮件、工单或外部编辑器；文档与实现应说明复制范围（通常为全部历史或当前筛选结果，二选一须在文档中固定）。
- 文档与 `03-ops` 用户说明中应写明：打开方式（状态栏点击）、Log 与状态栏单行摘要的关系（若状态栏仅显示最近一条，仍须在 Log 中可查历史）、**纯文本展示与复制按钮**的用途。

### Dock（停靠区 / 停靠容器）

**Dock** 指主窗口内一类**可停靠的窗口容器**（常用于承载工具面板、资源库、属性表、图层列表等），与**客户区**（主绘制/编辑区）并列，由布局系统吸附在主框架边缘。

| 属性 | 说明 |
|------|------|
| **本质** | **绘制窗口容器**：在界面说明中可描述为「承载某类工具或数据的停靠面板」，其**客户区**内为具体控件与内容；**与主画布/客户区**在文档中宜区分命名。 |
| **停靠位置** | 可停靠在**主窗口**的 **上**、**下**、**左**、**右** 四边之一（或产品允许的多边组合）；文档中应写明**默认停靠边**与是否允许用户拖拽换边。 |
| **折叠 / 展开** | 支持**展开**（显示完整面板）与**折叠**（隐藏面板主体）。 |
| **折叠后形态** | 折叠后**沿其当前停靠的那一边**（上/下/左/右）**缩小**，不再占用原面板宽度或高度；通常**收束为边缘上的一个小控件**（如**一条按钮**、窄条或「柄」），点击或悬停可再次**展开**。文档中应说明折叠态的可见形态（例如「收边为单按钮」）与恢复方式。 |

#### Dock 布局：缘条与显示区（多 Dock 同侧时推荐）

同一停靠侧若存在**多个** Dock（例如右侧同时有「块库」「属性」），或文档/实现要求折叠态与展开态在结构上**严格分离**时，宜采用下列布局约定；文档与代码命名应对齐。

| 区域 | 与停靠边的关系 | 职责 |
|------|----------------|------|
| **缘条**（edge strip，如 `dock-edge`） | 置于该侧**最贴近窗口边缘**的一列（或一行，若停靠在**上/下**边） | **仅**承载**折叠后**的收边控件（竖条/横条按钮等）；多个 Dock 同时折叠时，可在缘条内**沿边排列**多个按钮。 |
| **显示区**（display area，如 `dock-display`） | 与缘条**并列**，位于缘条与**主客户区（画布）**之间 | **仅**承载**展开态**的 Dock 面板内容；**不包含**折叠缘条按钮。 |

- **兄弟关系（强制语义）**：缘条与显示区在布局树中宜为**兄弟节点**（同一父级下的并列子区域），**勿**把「缘条上的折叠按钮」嵌在**某一个** Dock 面板组件内部作为唯一实现——否则多 Dock 同侧时难以共享显示区、缘条也难以与整块客户区对齐。
- **同侧多 Dock 共享空间**：同一缘条 + 同一显示区内可并列多个 Dock；**显示区**内各展开面板按产品约定分配空间（如纵向 `flex` 分栏）；**单独折叠其一**时，其余仍展开的面板应能**占满**该侧显示区剩余高度或宽度（文档中可写清「折叠其一后另一面板扩展」）。
- **左右顺序（实现提示）**：**左侧**停靠时，建议 DOM/阅读顺序为 `[ 缘条 | 显示区 | …客户区… ]`；**右侧**停靠时为 `[ …客户区… | 显示区 | 缘条 ]`，保证缘条始终贴**窗口最左/最右**（**上/下**边同理：缘条贴**最上/最下**）。文档说明中可用「左缘条 / 右缘条」等词与实现对齐。
- **折叠按钮与「沾满」方向（实现与文档）**：收边控件**不必**在无关维度上拉伸占满缘条。
  - **上/下**边停靠：缘条通常为**横向条带**；折叠后的按钮（或柄）**不需要**在**水平方向**上**左右沾满**整条缘条宽度（可按内容宽度或固定最小尺寸，多个按钮时沿边排列即可）。
  - **左/右**边停靠：缘条通常为**竖向条带**；折叠后的按钮**不需要**在**垂直方向**上**上下沾满**整条缘条高度（可按内容高度或固定最小尺寸，多个按钮时沿边排列即可）。

- **与浮动工具栏区分**：Dock **依附主窗口内边**，非独立顶层窗口；若实现为可拖拽成浮动窗，须在 GUI 说明中单独标注。
- **术语**：代码或文档中常用 `Dock`、`DockPanel`、`DockSite` 等命名，**含义以上述为准**；与操作系统「任务栏停靠」无关，勿混用。

#### Dock 边缘拖动与标题栏按钮（强制）

- **边缘拖动（调整尺寸）**：Dock 处于**展开**态时，其与**主客户区**之间的分界边（以及产品若定义的其它可调边）须支持用户用**鼠标拖动**改变 Dock 占用宽度或高度（典型为 **splitter / 分割条** 交互）。文档与 `03-ops` 用户说明中应写明：拖动位置、光标提示（如 `col-resize` / `row-resize`）、**最小/最大**尺寸或比例（若有）；Web/跨浏览器若存在限制，须如实说明。
- **标题栏控件**：每个 Dock 面板的 **标题栏**（或与之等价的标题行，承载面板名称的区域）上须提供至少两个按钮（名称可本地化，**职责须一致**）：
  - **折叠**：收起当前 Dock 面板主体（与上文「折叠/展开」语义一致，可与缘条联动）。
  - **最大化**：在当前停靠布局下将本 Dock **占满该侧可用显示区**（或产品约定的「该侧最大化」区域）；再次触发须能**还原**到最大化前尺寸/状态（或等价「还原」交互）。若技术栈无法实现「真最大化」或还原，须在文档中说明实际策略与阻塞原因。

## 定义（GUI 说明包含什么）

- **属于 GUI 说明**：主窗口与子窗口结构、菜单/工具栏/状态栏/停靠区、对话框与向导、画布/列表/属性区等**区域划分与命名**；**多语言菜单**、**主题菜单**、**状态栏与 Log 窗口**（见上文强制小节）；**Dock** 的**边缘拖动调尺寸**与标题栏 **折叠 / 最大化**（见上文 Dock 小节）；**鼠标/键盘/快捷键**与可见控件操作路径；**用户可见状态**（选中、禁用、加载中）与典型交互流程；若有权威 UI 几何/线框，引用 `00-concept/ui.svg` 等概念资产。
- **不属于 GUI 说明**（勿写入 GUI 专章来替代）：HTTP/gRPC/IPC/SDK 等**程序间接口**形态——此类归 `interface-design*.md`（参见 `sync-and-mapping.mdc` 对 `interface-design` 的界定）。

## 文档落点（与四阶段一致）

- **概念层**：`00-concept/product-design.md`、`software-design*.md` 中描述产品级界面与壳层划分；若有独立线框/图示，以 `ui.svg` 或概念资产为准。
- **逻辑层**：窗口内**模块协作与界面状态流**写在 `01-logic/` 的 `system-design.md` 或相应 `detailed-design-*.md`，避免与物理层字段级规格重复。
- **物理层**：**实现文件 ↔ 界面区域**的映射可写在对应 `02-physical/<target-id>/mapping.md` 或 `spec.md` 的「界面」小节（字段级仍以 `spec.md` 为权威）。
- **运维层**：用户可见的菜单、快捷键、**语言与主题切换**、**Log 窗口**与常见问题可在 `03-ops/user-manual.md` 中给操作说明。

## 同步义务

- **修改了窗口 GUI 实现**（例如 Vue/React/WPF/XAML/资源对话框等）：在力所能及范围内**回写或补齐**上述文档中与变更相关的 GUI 说明；若与 `sync-and-mapping.mdc` 的全量同步冲突，按该规则执行冲突流程。
- **回复中建议简要说明**：本轮是否涉及界面变更、已更新哪些文档路径（若未更新须写明阻塞原因）。

## 非目标

- 不强制为每个按钮写长篇文案；以**可检索、可追溯**为最低标准。
- 不将本规则升格为「唯一 UI 真相」：若仓库以 `ui.svg` 或产品文档为几何权威，物理层 UML **不得**替代该权威（与 `sync-and-mapping.mdc` 一致）。
